# Current Feature: Favorites Page

## Status

In Progress

## Goals

- Add a `/favorites` page listing all favorited items and collections in a compact, dev-focused list
- Add a star icon button to the TopBar linking to `/favorites`
- Protect the `/favorites` route (middleware, like `/profile` / `/settings`)
- Fetch all of the user's favorited items and favorited collections
- Compact list view (VS Code / terminal style — not cards)
- Each row shows: type icon, title, type badge, date added
- Separate sections for items and collections, each with a count
- Clicking an item opens the `ItemDrawer`; clicking a collection navigates to `/collections/[id]`
- Empty state when there are no favorites
- Sort by most recently favorited (`updatedAt`)

## Notes

- UI style: monospace / semi-monospace font, minimal padding, high density, subtle hover states, no cards or heavy borders — clean lines only
- Spec: `context/features/favorites-spec.md`
- Reuse the shared item-drawer hook (`src/components/items/use-item-drawer.ts`) / `ItemBrowser` pattern for the item rows
- Collections currently have no favorite toggle UI (deferred in the Collection Edit/Delete feature); this page only *reads* `isFavorite`. Demo data now has 6 favorite items + 2 favorite collections (set via Neon MCP)
- TopBar only exists on `/dashboard` today — confirm where the star button should live so it's reachable

## History

- Project setup and boilerplate cleanup
- Initial Next.js and Tailwind setup committed and pushed to GitHub
- Dashboard UI Phase 1 — ShadCN init (radix-nova preset) + button/input components, `/dashboard` route with layout shell, dark mode by default, display-only top bar (search + New Collection/New Item), placeholder sidebar & main area
- Dashboard UI Phase 2 — Functional sidebar: `DashboardShell` client wrapper owning sidebar state, collapsible `w-64`/`w-16` rail on desktop + radix `Dialog` drawer on mobile, `TopBar` PanelLeft toggle, `Sidebar` with collapsible Types (links to `/items/[type]`, colored icons, counts, active state) and Collections (Favorites + Recent) sections, bottom user avatar area, `/items/[type]` stub route
- Dashboard UI Phase 3 — Main workspace built out (server components, mock data imported directly): `page.tsx` composes a "Dashboard" header, `StatsSection` (4 `StatCard`s — total items, collections, favorite items, favorite collections), `CollectionsSection` (`CollectionCard` grid with color-coded left accent border + item-type icon strip), and two `ItemsSection` lists sharing `ItemRow` (Pinned items, 10 Recent items sorted by `updatedAt`). Shared `src/lib/type-presentation.ts` for type icon/color maps
- Prisma + Neon PostgreSQL Setup — **Prisma 7** (pinned `7.10`, npm `latest` is the v8 RC). New Rust-free `prisma-client` generator with required `output = "../src/generated/prisma"` (gitignored); datasource `url` removed from `schema.prisma` and moved to root `prisma.config.ts` (`import "dotenv/config"` + typed `env()`). `src/lib/prisma.ts` singleton uses the mandatory `PrismaPg` driver adapter. Schema: `User`/`Account`/`Session`/`VerificationToken` (Auth.js) + `Item`/`ItemType`/`Collection`/`Tag`/`ItemTag`, with FK indexes, `Item(userId, updatedAt)` composite index, `Tag(userId, name)` unique, cascade deletes on user-owned rows + `ItemTag`, `SetNull` on `Item.collectionId`, `Restrict` on `Item.typeId`. Initial migration `20260904034818_init` created with `migrate dev` (never `db push`) and applied to the Neon dev branch. `prisma/seed.ts` (wired via `migrations.seed`) upserts 7 system item types — run with `npx prisma db seed`. `scripts/test-db.ts` (`npm run test:db`) verifies Neon connectivity and prints row counts. `build` script runs `prisma generate && next build`. Follow-ups: run `prisma migrate deploy` on prod deploy; swap dashboard mock-data imports for Prisma queries.
- Seed Data — Added `bcryptjs` dependency (v3, bundled types). Rewrote `prisma/seed.ts` as an idempotent seed per `context/features/seed-spec.md`: upserts the 7 system item types (spec names/icons/colours, stable `type_*` ids), upserts demo user `demo@devstash.io` / `Demo User` (password `12345678` hashed with bcryptjs 12 rounds, `isPro: false`, `emailVerified: now`), then clears and rebuilds that user's collections/items/tags — 5 collections (React Patterns, AI Workflows, DevOps, Terminal Commands, Design Resources) and 18 items (snippets w/ `language`, prompts, `bash`/`yaml` commands + snippet, real-URL links). `scripts/test-db.ts` now fetches and prints the demo user + collections/items and asserts 5 collections / 18 items / 7 system types. Ran `npx prisma db seed`, `npm run test:db`, `npm run build`, `npm run lint` — all pass. Dashboard still reads `src/lib/mock-data.js`; wiring it to Prisma remains a follow-up.
- Dashboard Collections — Wire to Prisma — New `src/lib/db/collections.ts` (`getRecentCollections`) fetches the demo user's 6 most-recently-updated collections directly via Prisma, including each item's type, to compute per-collection item count, the most-used type (for the card's accent border), and the distinct type list (for the icon strip). `CollectionsSection` is now an async server component calling it instead of `mockCollections`; `CollectionCard` takes the real `CollectionWithStats` shape. `type-presentation.ts`'s hex-to-Tailwind palette extended with the seeded system-type colours (they differ from the old mock hexes) and `palette()` now accepts a nullable hex. `dashboard/page.tsx` marked `export const dynamic = "force-dynamic"` so it isn't statically baked in at build time with stale data. Item lists (Pinned/Recent) still read `mockItems` — out of scope, follow-up. `StatsSection`'s collection counts also still read mock data — follow-up. Ran `npm run build`, `npm run lint` — both pass; verified rendered HTML against the live Neon-seeded data.
- Dashboard Items — Wire to Prisma — New `src/lib/db/items.ts` (`getPinnedItems`, `getRecentItems`, `getItemStats`) fetches the demo user's items directly via Prisma, including each item's type and tags. `dashboard/page.tsx` now fetches pinned + recent (10) items itself (in parallel via `Promise.all`) and passes them to `ItemsSection`, replacing the `mockItems` filter/sort; when there are no pinned items the section renders nothing (unchanged existing `ItemsSection` behavior). `ItemRow` now takes the real `ItemWithType` shape — icon and a `CollectionCard`-style left accent border are both derived from the item's type colour via `palette()`; date formatting switched from parsing an ISO date-only string to formatting a real `Date`. Added `getCollectionStats` to `collections.ts`; `StatsSection` is now an async server component sourcing all four stat cards (total/favorite items, total/favorite collections) live via `getItemStats` + `getCollectionStats`, closing the follow-up left by the collections feature. Removed `type-presentation.ts`'s `typeTextColor` helper (mock-data-only lookup, no longer needed now that callers use `palette()` directly) along with its `mockItemTypes` import. Ran `npm run build`, `npm run lint` — both pass; verified rendered HTML against the live Neon-seeded data (18 items / 5 collections stats, type-coloured borders, no Pinned section since the seed data has no pinned items).
- Dashboard Stats & Sidebar — Wire to Prisma — Per `context/features/stats-sidebar-spec.md` (`StatsSection` was already DB-backed from the prior feature). New `getItemTypesWithCounts()` in `src/lib/db/items.ts` fetches system (+ any custom) item types with live per-type item counts, in a fixed display order (snippet, prompt, command, note, file, image, link). `src/lib/db/collections.ts` factored its query/mapping into a shared `fetchCollectionsWithStats` helper and added `getSidebarCollections()` (all of the user's collections, unlike the capped `getRecentCollections`). `Sidebar` now takes `itemTypes`/`collections` as props instead of `mock-data.ts`, sourced by `dashboard/layout.tsx` (marked `force-dynamic`) and threaded through `DashboardShell` to both the desktop and mobile sidebars; it reuses `type-presentation.ts`'s icon/colour palette instead of its own duplicate maps (dropped `TYPE_ICONS`/`TYPE_COLORS`). `type-presentation.ts`'s palette gained a `dot` (solid `bg-*`) variant per colour. Non-favorite ("Recent") collections in the sidebar now show a colored dot keyed to their most-used item type instead of an item count; favorites keep their star. Added a "View all collections" link under the list, pointing to a new `/collections` stub page (mirrors the existing `/items/[type]` placeholder style). `/items/[type]/page.tsx` switched from `mockItemTypes` to `getItemTypesWithCounts()` (dropped `generateStaticParams`, marked `force-dynamic`) so the sidebar's DB-derived links (e.g. `/items/snippet`, singular/lowercase) resolve correctly. Also updated `prisma/seed.ts`: added `isFavorite` to `SeedCollection` and marked React Patterns and Terminal Commands as favorites (the seed previously left every collection non-favorite, so the sidebar's Favorites group had nothing to show) — re-ran `npx prisma db seed`. Ran `npm run build`, `npm run lint` — both pass; verified rendered HTML against the live Neon-seeded data (sidebar type counts sum to 18, 2 favorite + 3 recent collections, `/items/[type]` and `/collections` respond 200, unknown type 404s).
- Add Pro Badge to Sidebar — Per `context/features/add-pro-badge-sidebar.md`. Added the ShadCN `Badge` component (`src/components/ui/badge.tsx`, installed via `npx shadcn add badge`; fixed the CLI's generated import from a nonexistent `cn` package to the project's `@/lib/utils` alias, and removed the stray `cn` dependency it had added to `package.json`). `Sidebar.tsx` now renders a subtle outline "PRO" badge (`text-[10px]`) next to the Files and Images entries in the Types list, matched by a `PRO_TYPE_NAMES` set against `type.name`; hidden when the sidebar is collapsed, same as the item count it sits beside. Ran `npm run build`, `npm run lint` — both pass; verified visually in the browser via Playwright screenshot.
- Codebase Quick-Win Cleanup — Three low-risk fixes from a full-codebase `code-scanner` audit (auth-related findings excluded, since auth isn't implemented yet). Added `@@index([userId, updatedAt])` to `Collection` in `prisma/schema.prisma`, matching `Item`'s existing composite index, applied via a real migration (`prisma migrate dev`, `20260905041214_add_collection_updated_at_index`) so dev/prod stay syncable. New `src/lib/db/user.ts` exports `getDemoUserId`, wrapped in React's `cache()` so the ~7 identical `prisma.user.findUnique` calls per dashboard render collapse into one per request; `items.ts` and `collections.ts` now import it instead of each re-deriving the demo user id (plain Prisma Client calls only, no raw SQL). Added scoped `getItemTypeByName()` (`items.ts`) and `getCollectionCount()` (`collections.ts`) so the `/items/[type]` and `/collections` placeholder pages no longer fetch full datasets just to do a 404 check or print a count. Ran `npm run build`, `npm run lint` — both pass; verified locally that `/dashboard`, `/items/snippet` (200, count 4), `/items/nonexistent` (404), and `/collections` (200, count 5) all behave correctly.
- Auth Phase 1 — NextAuth v5 + GitHub Provider — Per `context/features/auth-phase-1-spec.md`. Installed `next-auth@beta` + `@auth/prisma-adapter`. Split-config: `src/auth.config.ts` (adapter-free slice, GitHub provider) + `src/auth.ts` (full instance, PrismaAdapter, JWT sessions, `jwt`/`session` callbacks threading `user.id`). API route re-exports `handlers`. `src/proxy.ts` (Next 16 middleware) guards `/dashboard`. `src/types/next-auth.d.ts` augments `Session`/`JWT` with `id`.
- Auth Phase 2 — Credentials provider (`authorize` in `src/auth.ts`: Zod parse → `prisma.user.findUnique` → `bcrypt.compare`), `POST /api/auth/register` (API route, Zod, bcrypt.hash 12, 201/409/400). Shared `src/lib/validations/auth.ts`. Added `zod` v4.
- Auth Phase 3 — Auth UI (custom `/sign-in`, `/register`, `/profile` stub; `SignInForm`/`RegisterForm`/`GitHubIcon`; `UserAvatar` + `initials()`; `SidebarUser` with sign-out dropdown; `pages.signIn = "/sign-in"`).
- Email Verification on Register — Resend + `VerificationToken` table (no schema change); `sendVerificationEmail`, token helpers, `getBaseUrl`; register mints+sends, `GET /api/auth/verify-email`, `POST /api/auth/resend-verification` (60s debounce, no enumeration); `authorize` throws `UnverifiedEmailError` on correct-password-but-unverified.
- Email Verification Toggle Flag — `src/lib/auth-flags.ts` `emailVerificationEnabled()` (env `EMAIL_VERIFICATION_ENABLED`, off only when literally `"false"`), wired into register / `authorize` / resend / sign-in banner copy.
- Forgot Password — `pwreset:<email>`-namespaced reuse of `VerificationToken` (no schema change); `create/latest/consumePasswordResetToken`, `sendPasswordResetEmail`; `POST /api/auth/forgot-password` (no enumeration, 60s debounce) + `POST /api/auth/reset-password` (consume → bcrypt.hash → `updateMany`, stamps `emailVerified` if null); `/forgot-password` + `/reset-password` pages + forms; `?reset=1` sign-in banner.
- Rate Limiting for Auth — `@upstash/ratelimit` + `@upstash/redis`. `src/lib/rate-limit.ts` (lazy Redis singleton, disabled+warn when env unset, per-name memoised sliding-window limiter, `getClientIp`, fail-open `checkRateLimit`, `rateLimitResponse`/`credentialsRateLimitResponse`). Wired into register (3/1h IP), forgot-password (3/1h IP), reset-password (5/15m IP), resend-verification (3/15m IP+email), and login via a POST wrapper in the `[...nextauth]` route (5/15m IP+email). Inline `role="alert"` frontend messages.
- GitHub Sign-In via Server Action — New `src/actions/auth.ts` (`"use server"`) `signInWithGitHub(redirectTo)` → `signIn("github", { redirectTo })` from `@/auth`; `SignInForm` GitHub button calls it instead of the client `signIn`. First file under `src/actions/`.
- Profile Page — `src/lib/db/profile.ts` (`getProfileUser` session-scoped, `hasPassword` derived, never returns the hash; `getProfileStats`). `TYPE_ORDER` exported + `compareTypeOrder`. Real `/profile` page (avatar, badges, `ProfileStats`, Account section). `ChangePasswordForm` + `DeleteAccountDialog` (radix `Dialog`). `POST /api/auth/change-password` + `POST /api/auth/delete-account` (auth-gated, cascade delete + `verificationToken.deleteMany`). `changePasswordSchema` + `deleteAccountSchema`.
- Items List View — `getItemsByType(typeId)` in `src/lib/db/items.ts`; `src/components/dashboard/ItemCard.tsx` grid card; `/items/[type]/page.tsx` renders a real type-filtered grid + empty state + live count.
- Vitest Unit Testing Setup — `vitest@^5`, `vitest.config.mts` (node env, `src/{actions,lib}/**/*.test.ts`, `@`→`./src` alias). `test` / `test:watch` scripts. 8 test files / 37 tests. Docs updated (`CLAUDE.md`, `context/ai-interaction.md`, `context/coding-standards.md`).
- Item Listing — Three-Column Grid — `/items/[type]/page.tsx` grid `sm:grid-cols-2 lg:grid-cols-3`.
- Item Drawer — Right-side slide-in `Sheet` item detail view. `src/components/ui/sheet.tsx` (on unified `radix-ui` Dialog). `getItemDetail(id)` + `GET /api/items/[id]`. `src/components/items/ItemBrowser.tsx` (client wrapper, drawer state, fetch + cache) + `src/components/items/ItemDrawer.tsx`. `ItemsSection` + `items/[type]/page.tsx` delegate to `<ItemBrowser>`.
- Item Drawer — Edit Mode — `src/lib/validations/item.ts` (`updateItemSchema`, Zod v4) + tests. `updateItem(id, data)` in `src/lib/db/items.ts` (demo-user-scoped, ownership guard, wholesale tag replace). `src/actions/items.ts` (`"use server"` `updateItem` action) + tests. `ItemDrawer` gains `editing` state + `<ItemEditForm>` (type-specific fields, `sonner` toasts, inline `fieldErrors`). `ItemBrowser` `handleSaved` updates cache + `router.refresh()`. New dep `sonner@^2` + `src/components/ui/sonner.tsx` mounted in `src/app/layout.tsx`.
- Item Delete — `deleteItem(id)` + `deleteItem` server action; `AlertDialog` primitive; drawer trash button → confirm → toast → `onDeleted` evicts cache + refreshes.
- Item Create — `NewItemDialog` (pill type selector, type-conditional fields), `createItemSchema` + `createItem` action + `createItem` query, `TopBar` owns `newItemOpen`.
- Code Editor (Monaco), Markdown Editor, File & Image Upload (R2), Image Gallery View, File List View, Card Quick-Copy Icon, Low-Risk Fixes Batch, Code Breakup refactor — see git history.
- Collection Create — Top-bar "New Collection" opens a create modal. `createCollectionSchema` + `createCollection` query + `POST /api/collections` (API route, not a server action — deliberate divergence from item-create). `NewCollectionDialog` modeled on `NewItemDialog`. Tests: `collection.test.ts` (4) + `collections.test.ts` (3).
- Item ↔ Collections — Many-to-Many + Form Picker — `CollectionItem` join model, `getCollectionOptions` + `GET /api/collections`, `CollectionPicker` in the item forms, migration `20260906120000_item_collections_many_to_many`.
- Collections Pages — List & Detail — real `/collections` list + new `/collections/[id]` detail (`getCollectionById` + `getItemsByCollection`, `<ItemBrowser layout="grid">`). `CollectionCard` became a `<Link>`; sidebar collection rows + "View all" became links.
- Collection Edit / Delete (+ Favorite placeholder) — Per `context/current-feature.md`. **Data layer** (`src/lib/db/collections.ts`): `updateCollection(id, data)` (demo-user scoped; ownership folded into a `updateMany` `where`, `count === 0` → `null`, else re-reads the fresh `CollectionSummary`) and `deleteCollection(id)` (`deleteMany` `where: { id, userId }`, returns `count > 0`). Deleting a collection does **not** touch its items — only the `CollectionItem` join rows cascade away (`onDelete: Cascade`, already in schema). **Validation** (`src/lib/validations/collection.ts`): `updateCollectionSchema` = alias of `createCollectionSchema` (+ `UpdateCollectionInput`). **API** — new `src/app/api/collections/[id]/route.ts` with `PATCH` (auth → `readJsonBody`/`INVALID_JSON` guard → `updateCollectionSchema.safeParse` → `validationErrorResponse` → `updateCollection`, `null` → 404, else `{ success, data }`) and `DELETE` (auth → `deleteCollection`, `false` → 404, else `{ success, data: { id } }`); same `{ success, error, details? }` shape + `src/lib/api/request.ts` helpers as `POST /api/collections` (kept an API route, not a server action, mirroring the create-collection decision). **UI primitive** — new `src/components/ui/dropdown-menu.tsx`, hand-written on `radix-ui`'s `DropdownMenu` (Root/Trigger/Content/Item/Separator; `Item` has a `destructive` variant), same pattern as `dialog.tsx` / `alert-dialog.tsx`; uses `bg-popover` tokens. **Components**: `EditCollectionDialog` (`"use client"`, modeled on `NewCollectionDialog` — shared `Dialog` primitive + `Field` + `textareaClass`, render-phase re-seed from props on open, `postJson(url, body, "PATCH")`, inline `fieldErrors` from `details`, `sonner` toast, `router.refresh()`, optional `onSaved`); `DeleteCollectionDialog` (`AlertDialog` confirmation, `postJson(url, undefined, "DELETE")`, treats a **404 as already-done** so a stale tab still proceeds, then calls `onDeleted` — it does **not** flip its own `open`, so there's no close-vs-navigate race); `CollectionActionsMenu` (the three-dots dropdown for cards — Edit / Favorite / Delete; `onDeleted` = `setDeleteOpen(false)` + `router.refresh()`); `CollectionDetailActions` (Favorite / Edit / Delete icon buttons for the `/collections/[id]` header; `onDeleted` = `setDeleteOpen(false)` + `router.push("/collections")`). **Favorite is a placeholder everywhere — an inert icon button / menu item, no handler** (deferred to a later feature). **`CollectionCard`** restructured from a single `<Link>` to the codebase's stretched-link pattern: a `.group.relative` wrapper holding an `absolute inset-0` `<Link>` overlay (aria-label "Open <name>") under `pointer-events-none relative z-10` content, with `<CollectionActionsMenu>` as an `absolute top-3 right-3 z-10` sibling (its own `<button>` trigger stays clickable); title row got `pr-8` to clear the button. One change covers the dashboard `CollectionsSection` and the `/collections` list. **`/collections/[id]/page.tsx`** — header wrapped in a `flex items-start justify-between` row with `<CollectionDetailActions collection={collection}>` (the existing `getCollectionById` `CollectionSummary` already matches the needed `{ id, name, description, isFavorite }`). **`src/lib/post-json.ts`** — `postJson` gained an optional 3rd `method` arg (default `"POST"`) so it can also do PATCH/DELETE; existing callers unaffected. First cut had `DeleteCollectionDialog` flip its own `open` then call `onDeleted()`, which raced with the AlertDialog close and sometimes swallowed the `router.push` (user-reported "does not redirect after delete"); reworked so the parent owns what happens next. **Tests +9** → `npm run test` **187 pass** (was 178): `collection.test.ts` — `updateCollectionSchema` is the same object as `createCollectionSchema` + trims/requires name/normalises description (×2); `collections.test.ts` — `updateCollection` (no demo user → `null`/no write; happy path asserts exact `updateMany` + re-read `findFirst` shape; `count: 0` → `null` without re-reading) and `deleteCollection` (no demo user → `false`/no write; scoped `deleteMany`; foreign id → `false`) (×6); `post-json.test.ts` — passes the `method` arg through (×1). `npm run lint`, `npm run build` — both green (`/api/collections/[id]` shows as a new `ƒ` route). Browser-verified (Playwright, demo session, live Neon): detail-page Edit → modal pre-filled → PATCH 200 → header + toast update; detail-page Delete → confirm → toast + **redirect to `/collections`**, list down one, **all 18 items intact** (3 `CollectionItem` join rows cascaded); card three-dots menu (dashboard + `/collections`) → Edit refreshes the card in place, Delete stays on the page with the list refreshed, Favorite is inert (no console error); card body click still navigates to the detail page. Dev DB re-seeded afterward. Deferred (raised earlier, still open): shared `BackLink` / `pluralize()` / `ListPageShell` extraction across `/items/[type]`, `/collections`, `/collections/[id]`; collection favorite toggle; adding items to a collection from these pages; pagination.
- Global Search / Command Palette — Per `context/features/global-search-spec.md`. Cmd/Ctrl+K palette with all matching done client-side. **Data**: `getAllItems()` added to `src/lib/db/items.ts` (every demo-user item, `getRecentItems` without the `take`); `getSearchCollections()` + `SearchCollection` (`{ id, name, itemCount }` via `_count`) added to `src/lib/db/collections.ts`; new `src/lib/db/search.ts` `getSearchIndex()` = `Promise.all([getAllItems(), getSearchCollections()])` → `{ items, collections }`. Fetched in the already-`force-dynamic` `dashboard/layout.tsx` (so `router.refresh()` after any create/edit/delete re-runs it — no separate API route, no staleness) and threaded `DashboardShell` → `TopBar` as `searchItems` / `searchCollections`. **UI primitive**: new dep `cmdk@^1.1.1`; `src/components/ui/command.tsx` wraps cmdk (`Command` / `CommandDialog` / `CommandInput` / `CommandList` / `CommandEmpty` / `CommandGroup` / `CommandItem` / `CommandSeparator`) in the codebase's radix-ui/`cn`/token style, `CommandDialog` built on the shared `Dialog` primitive; `src/components/ui/dialog.tsx` `DialogContent` gained an opt-out `showCloseButton` prop (default `true`, existing callers unaffected) so the palette has no X. **Drawer reuse**: extracted `ItemBrowser`'s drawer state + `/api/items/[id]` detail-fetch/cache + `handleSaved` / `handleDeleted` into a shared hook `src/components/items/use-item-drawer.ts` (`useItemDrawer()`); `ItemBrowser` now just consumes it (mechanical, no behaviour change). **Palette**: `src/components/dashboard/CommandPalette.tsx` — `Items` + `Collections` groups; item rows show the type icon (`TYPE_ICON` + `palette().text`) + a whitespace-collapsed 100-char content/url/description preview; collection rows show `N items`; picking an item calls the shared `useItemDrawer().select` (opens the same `ItemDrawer`, full detail incl. favorite/pin/tags/collections), picking a collection `router.push`es `/collections/[id]`. `TopBar` search field became a `<button>` (Search icon + "Search items..." + `⌘K` kbd) that opens the palette; `TopBar` owns `paletteOpen` + a global `keydown` listener (`(metaKey||ctrlKey) && key==='k'`, `preventDefault`, toggle). **Search quality**: cmdk's built-in scorer is a subsequence match, so short queries like "test" matched almost every row (letters scattered through titles / the content previews loaded into `keywords`). Replaced via `<Command filter>` (new passthrough on `CommandDialog`) with `src/lib/command-filter.ts` `commandFilter(value, search, keywords?)` — every whitespace token in the query must be a literal case-insensitive **substring** of one of the row's fields or the row is dropped (score 0); ranking favours the first field (title) and earlier positions; score is the per-token mean. `CommandItem` `keywords` are `[title, typeName, ...tags, preview]` for items and `[name]` for collections; `value` stays `"<title/name> <id>"` for cmdk selection uniqueness. Follow-up (this session): `commandFilter` reworked to a **binary** match (score `1`/`0`, no field/position ranking) so cmdk's score-descending sort no longer reorders the result list on each keystroke — rows now stay in render order (items newest-first, then collections); the inert `useMemo` (`CommandPalette` previews) and `useCallback`s (`use-item-drawer`) were removed since the React Compiler is not enabled. **Tests +14** → `npm run test` **200 pass** (was 187), then **199** after the binary-filter rework swapped two ranking tests for one. `npm run lint`, `npm run build` — both green (`cmdk` bundled into the dashboard route). Browser-verified (Playwright, demo session, live Neon): ⌘K and clicking the search field both open it; grouped Items/Collections with icons + counts; ↑↓/Enter selection; item → drawer with full detail; collection → navigation to `/collections/[id]`; "No results found." empty state; stable ordering confirmed (`"e"` → 17 items in newest-first order + 4 collections; `"docs"` → 3 rows in insertion order). Note: the palette (and the TopBar) only exist on `/dashboard`. Deferred: debounced re-fetch / pagination for large libraries (whole dataset is shipped to the client); palette availability outside `/dashboard`.
- Command Palette — Stable Result Ordering (follow-up fix) — `src/lib/command-filter.ts` `commandFilter` reworked from a graded field/position score to a **binary** `1`/`0` match. cmdk sorts visible rows by descending filter score, so the graded score made the result list reorder on every keystroke (title hits jumping above body hits, earlier matches above later ones) — flicker. With one score for every match, `Array.prototype.sort`'s stability leaves rows in render order: items newest-first (DB `updatedAt desc`), then collections. Same match semantics as before (per-token, case-insensitive, literal substring, all tokens required); the fields are just concatenated before the substring test. Also removed the now-inert `useMemo` (`CommandPalette` `previews` map) and the three `useCallback`s (`use-item-drawer` `select` / `handleSaved` / `handleDeleted`) — the React Compiler isn't enabled, and none of them gated a re-render (`ItemDrawer` isn't `React.memo`, cmdk re-renders every row regardless). `command-filter.test.ts` swapped its two ranking tests for one asserting equal scores → `npm run test` **199 pass** (was 200). `npm run lint`, `npm run build` green. Browser-verified (Playwright, live Neon): `"e"` → 17 items newest-first + 4 collections, `"docs"` → 3 rows in insertion order, no console errors.
- Pagination — Per `context/features/pagination-spec.md`. Numbered pagination (page links + prev/next) on `/items/[type]`, `/collections/[id]` and the `/collections` list; each page fetches only its slice (`skip`/`take` + a `count`), never the whole dataset. **New `src/lib/pagination.ts`**: constants `ITEMS_PER_PAGE = 21`, `COLLECTIONS_PER_PAGE = 21`, `DASHBOARD_COLLECTIONS_LIMIT = 6`, `DASHBOARD_RECENT_ITEMS_LIMIT = 10`; `Paginated<T>` = `{ items, page, pageCount, total }`; `parsePageParam(raw)` (→ 1-based int, junk/`0`/`-3`/`2.5` → 1); `paginate(total, page, size)` → clamped `{ page, pageCount, skip, take }` (out-of-range clamps to the last page, never an empty page); `pageWindow(page, pageCount)` → page numbers with an `ELLIPSIS` marker where the run skips (all pages if ≤ 7). Full `pagination.test.ts`. **New `src/components/ui/pagination.tsx`** — server component, built on `Button` (`asChild` + `next/link`) + `ChevronLeft/Right`; current page is a filled `Button` with `aria-current`, others outline links, prev/next render as **disabled `<button>`s** (not links) at the ends, `page 1` links use the bare `basePath` (no `?page=1`); returns `null` for `pageCount <= 1`. **Data layer** (`src/lib/db/items.ts`): new private `getItemsPage(where, page)` runs `prisma.item.count` + a `findMany({ skip, take })`; `getItemsByType(typeId, page)` and `getItemsByCollection(collectionId, page)` now return `Paginated<ItemWithType>` (was `ItemWithType[]`). (`src/lib/db/collections.ts`): `fetchCollectionsWithStats` gained a `{ skip?, take? }` range arg (skip 0 omitted so it's absent on page 1); new `getCollectionsPage(page)` → `Paginated<CollectionWithStats>` (count + paged fetch); `getRecentItems` / `getRecentCollections` defaults now reference the `DASHBOARD_*` constants, and `dashboard/page.tsx` + `CollectionsSection` pass them instead of literal `10` / `6`. `getSidebarCollections` still returns all (the sidebar needs the full list). `getCollectionCount` left untouched (pre-existing, unused). **Pages** — all three take `searchParams: Promise<{ page?: string | string[] }>`, resolve `params` + `searchParams` together, feed `parsePageParam` into the query, switch the count line + empty-state check from `items.length` to `total`, and drop `<Pagination page pageCount basePath>` after `<ItemBrowser>` / the card grid (`basePath` = `/items/${type}`, `/collections/${id}`, `/collections`). `/collections/page.tsx` swapped `getSidebarCollections()` for `getCollectionsPage()`. **Tests +16** → `npm run test` **215 pass** (`items.test.ts` rewritten for the `Paginated` shape + `item.count` mock + skip/take + clamp cases; `collections.test.ts` +`getCollectionsPage` block + `collection.count` mock; new `pagination.test.ts`). `npm run lint`, `npm run build` green (routes unchanged, all already `ƒ`). Browser-verified (Playwright, demo session, live Neon) with temporary rows then cleaned up (DB back to 5 collections / 18 items): `/items/link` (31 items → 21 + 10 across 2 pages, prev disabled on p1 / next disabled on p2, `?page=99` clamps to p2), `/collections/[id]` (28 items → 21 + 7), `/collections` (25 collections → 21 + 4); `/items/snippet` (4 items) shows no pager. Note: the pager only renders once a list exceeds 21, so with the current seed data none of the three pages show it. Deferred: shared `ListPageShell` / `BackLink` / `pluralize()` extraction across the three list pages (raised earlier, still open); the command palette still ships its whole dataset to the client (separate from these server-paginated pages).
- Settings Page + Sidebar Link — New protected `/settings` route (`src/app/settings/page.tsx`, `force-dynamic`, `title` "Settings · DevStash"), added to the `src/proxy.ts` middleware matcher (`/settings` + `/settings/:path*`, alongside the existing `/profile` entries). The sidebar user dropdown (`SidebarUser`) gained a **Settings** item (lucide `Settings` icon) between Profile and Sign out — hidden when the rail is collapsed like the rest of that menu. The account actions moved off `/profile` onto `/settings`: `ChangePasswordForm` + `DeleteAccountDialog` `git mv`d from `src/components/profile/` to `src/components/settings/` (only the profile page imported them; `ProfileStats` stays in `profile/`; `ChangePasswordForm`'s doc comment updated profile→settings). The profile page keeps the avatar/badges/`ProfileStats` and its Account `<section>` is now a single card that links through to `/settings`. **New `requireProfileUser(callbackPath)`** in `src/lib/db/profile.ts` folds the `auth()` → session-id → `getProfileUser` → `redirect("/sign-in?callbackUrl=…")` dance that `/profile` and `/settings` were both duplicating into one helper — covers both failure cases (no session, session whose user row is gone), returns a `ProfileUser`, and `encodeURIComponent`s the callback path (the old inline code didn't). `profile.ts` now imports `redirect` (next/navigation) + `auth` (@/auth); its header comment broadened to "`/profile` and `/settings` pages". The profile page now awaits `requireProfileUser` then `getProfileStats(user.id)` sequentially instead of a `Promise.all` — one extra Neon round-trip on a `force-dynamic` page, judged worth the dedup. **New `src/lib/db/profile.test.ts`** (+3): no session → redirects with an encoded `callbackUrl` and never hits Prisma; session but missing user row → redirects; happy path → returns the user with `hasPassword` derived and no `password` field, no redirect (mocks `@/auth`, `@/lib/prisma`, and `next/navigation`'s throwing `redirect`). Ran `npm run test` **218 pass** (was 215), `npm run lint`, `npm run build` — all green (`/settings` builds as a `ƒ` route). Browser-verified (Playwright, demo session, live Neon): dropdown → Settings navigates to `/settings`; both the change-password card (form expands) and delete-account card render; profile page renders with the "Go to settings" link and no inline account actions; unauthenticated `GET /settings` and `/profile` both 302 → `/sign-in?callbackUrl=…` via the middleware. Deferred: the API routes `POST /api/auth/change-password` + `/delete-account` still do their own `auth()` guard (they return JSON 401, not a redirect, so `requireProfileUser` doesn't fit); a settings link on the profile page's Account card is the only cross-link (no reverse link from settings back to profile beyond "Back to dashboard").
- Editor Preferences Settings — Per `context/features/editor-settings-spec.md`. An "Editor preferences" card on `/settings` with five auto-saving controls (no save button): font size (`select`, 12/13/14/16/18px), tab size (`select`, 2/4/8), theme (`select`, `vs-dark` / `monokai` / `github-dark`), and word-wrap + minimap `role="switch"` toggles. Defaults: fontSize 13, tabSize 2, wordWrap **on**, minimap **off**, theme `vs-dark` (the last three per spec). **Schema**: `User.editorPreferences Json?` (`JSONB`, nullable — `null` means "use defaults"; seed doesn't touch it), migration `20260907171910_add_user_editor_preferences` via `prisma migrate dev` on the Neon dev branch. **`src/lib/editor-preferences.ts`** (pure, tested): `EditorPreferences` type, `DEFAULT_EDITOR_PREFERENCES`, `FONT_SIZE_OPTIONS` / `TAB_SIZE_OPTIONS` / `EDITOR_THEMES` / `EDITOR_THEME_OPTIONS`; `normalizeEditorPreferences(raw: unknown)` — the single gate turning the untyped JSON column (or a partial patch) into a complete valid object, dropping any field not in its option list; `monacoThemeName(theme)` → `devstash-<theme>`; `toMonacoEditorOptions(prefs)` → `{ fontSize, lineHeight: round(fontSize*1.55), tabSize, wordWrap: "on"|"off", minimap: { enabled } }`. **Validation** (`src/lib/validations/editor-preferences.ts`): `editorPreferencesSchema` — all fields required (the form sends the full object every change), numerics `.refine`d against the option lists, `theme` a `z.enum(EDITOR_THEMES)`; `UpdateEditorPreferencesInput`. **Data layer** (`src/lib/db/editor-preferences.ts`): `getEditorPreferences(userId: string | null | undefined)` — returns `DEFAULT_EDITOR_PREFERENCES` (no query) when no id, else `findUnique` → `normalizeEditorPreferences`; `updateEditorPreferences(userId, data)` — `prisma.user.update` (`data` cast `as unknown as Prisma.InputJsonObject` — a typed interface doesn't satisfy `InputJsonValue`'s index-signature) then re-normalise. **Session-scoped, not demo-user-scoped** — unlike the dashboard data layer, these key off the real signed-in user id (like `src/lib/db/profile.ts`). **Server action** (`src/actions/editor-preferences.ts`, `"use server"`): `updateEditorPreferences(input: unknown)` → `auth()` guard → `editorPreferencesSchema.safeParse` (→ `fieldErrors`) → query (imported aliased as `updateEditorPreferencesQuery`) → `{ success, data }` / `{ success, error }`; try/catch → generic error. **Context** (`src/components/editor-preferences/EditorPreferencesProvider.tsx`, `"use client"`): holds `preferences` seeded from an `initial` prop; `update(patch)` optimistically sets, calls the action, then `toast.success("Editor preferences saved")` or rolls back + `toast.error`. On success it does **not** re-apply `result.data` (would clobber a newer optimistic change mid-flight). A `latest` **ref** mirrors the newest optimistic state so back-to-back `update()` calls build `next` from it, not a stale render closure — this was a real bug in the first cut (rapid theme/font/minimap changes clobbered each other; DB ended up with only the last field's change on a stale base). Two hooks: `useEditorPreferences()` (strict — throws outside a provider; for the settings form) and `useEditorPreferencesValue()` (lenient — returns `DEFAULT_EDITOR_PREFERENCES` outside a provider; for `CodeEditor`). **UI** (`src/components/settings/EditorPreferencesForm.tsx`): local `selectClass` + a `Row` layout helper + a `Toggle` (`role="switch"` button, `aria-checked`); every control's `onChange` calls `update({ field })`; inputs `disabled={saving}`. **Monaco** (`src/components/items/monaco-themes.ts`): `registerMonacoThemes(monaco)` defines `devstash-vs-dark` (the app-tuned theme that previously lived inline in `CodeEditor` as `defineTheme`), plus compact hand-rolled `devstash-monokai` / `devstash-github-dark` palettes (Monaco only ships `vs` / `vs-dark` / `hc-*`). **`CodeEditor.tsx`**: dropped the module-level `LINE_HEIGHT` const + inline `defineTheme`; `estimateHeight(value, lineHeight)` now takes the derived line height; reads `useEditorPreferencesValue()`, spreads `toMonacoEditorOptions(preferences)` over the static `options` (so `fontSize` / `tabSize` / `wordWrap` / `minimap` / `lineHeight` are preference-driven — previously hard-coded 13 / 2 / "off" / disabled / 20), and sets `theme={monacoThemeName(preferences.theme)}` + `beforeMount={registerMonacoThemes}`. **Provider mounting**: `src/app/settings/page.tsx` fetches `getEditorPreferences(user.id)` and wraps the new card's form in `<EditorPreferencesProvider initial={…}>`; `src/app/dashboard/layout.tsx` awaits `auth()` first, then `Promise.all([… , getEditorPreferences(session?.user?.id)])` and wraps `<DashboardShell>` in the provider — so the Monaco editors in the item drawer / create+edit forms pick up the preferences. Cross-route live sync isn't a goal (the settings page and the dashboard each have their own provider instance); a change on `/settings` reaches an open dashboard editor after the next `router.refresh()` / navigation. **Tests +20** → `npm run test` **238 pass** (was 218): `editor-preferences.test.ts` (normalize: null/non-object → defaults, valid-value merge, drops out-of-range/wrong-type/unknown-theme, all three themes accepted; `toMonacoEditorOptions` mapping incl. wordWrap on/off; `monacoThemeName`); `validations/editor-preferences.test.ts` (well-formed passthrough, rejects off-list font/tab size, unknown theme, non-boolean toggle, missing fields); `db/editor-preferences.test.ts` (no id → defaults + no query; normalises the stored blob; null column → defaults; `update` writes the payload + returns the normalised round-trip — Prisma singleton mocked); `actions/editor-preferences.test.ts` (unauth → error + no DB; invalid payload → `fieldErrors`; happy path calls the query with `session.user.id` + validated data; query throws → generic error — `@/auth` + query mocked). `npm run lint`, `npm run build` — both green (`/settings` still `ƒ`; `prisma generate` picks up the new column). Browser-verified (Playwright, demo session, live Neon): settings card renders with defaults; three rapid changes (font→18, tab→2, minimap off) each toast "saved" and all five values persist correctly to `User.editorPreferences` (the ref fix — before it, they raced); reload re-hydrates the form from the DB; opening a snippet in the dashboard item drawer shows Monaco with the Monokai palette + larger font; no console errors. Reset the demo user's `editorPreferences` to `NULL` afterward. Deferred: cross-route live preference sync without a refresh; per-item / per-language editor overrides; exposing font family or line-number visibility; a preview editor inside the settings card.
