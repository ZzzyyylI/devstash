# Current Feature: Items List View

<!-- Feature Name -->

Dynamic items listing page at `/items/[type]` that displays type-filtered items.

## Status

<!-- Not Started|In Progress|Completed -->

In Progress

## Goals

<!-- Goals & requirements -->

- Build out the existing dynamic route `/items/[type]` (currently a placeholder) to render a real type-filtered item list
- Fetch the demo user's items filtered by the resolved item type via Prisma (`src/lib/db/items.ts`), following existing DB-layer patterns
- Display items in a responsive grid of `ItemCard` components — two columns on medium screens and up, one column below
- Each card has a left border colored by the item type (derive via `palette()` in `src/lib/type-presentation.ts`, matching `ItemRow` / `CollectionCard`)
- Keep the existing 404 behavior for unknown types and the "Back to dashboard" link
- Follow existing codebase patterns (async server component, `force-dynamic`, `src/lib/db` fetchers, ShadCN/Tailwind styling)

## Notes

<!-- Any extra notes -->

- Spec: `context/features/item-list-view-spec.md`
- The route already exists as a placeholder: `src/app/items/[type]/page.tsx` (uses `getItemTypeByName`, `force-dynamic`, `notFound()` on unknown type). Sidebar type links point here with singular/lowercase names (e.g. `/items/snippet`).
- New: an `ItemCard` component (grid card) — the spec names `ItemCard`; the codebase currently only has the compact `ItemRow` for the dashboard lists. Decide whether to add `ItemCard` alongside it or adapt.
- New DB fetcher needed: items filtered by type id for the demo user (add to `src/lib/db/items.ts`, reuse `ITEM_INCLUDE` / `toItemWithType`).
- Reuse `TYPE_ICON` / `palette()` / `FALLBACK_ICON` from `src/lib/type-presentation.ts` for icon + left-accent color.
- Verify with `npm run build` and `npm run lint`; check rendered output against live Neon-seeded data.

## History

<!-- Keep this updated. Earliest to latest -->

- Project setup and boilerplate cleanup
- Initial Next.js and Tailwind setup committed and pushed to GitHub
- Dashboard UI Phase 1 — ShadCN init (radix-nova preset) + button/input components, `/dashboard` route with layout shell, dark mode by default, display-only top bar (search + New Collection/New Item), placeholder sidebar & main area
- Dashboard UI Phase 2 — Functional sidebar: `DashboardShell` client wrapper owning sidebar state, collapsible `w-64`/`w-16` rail on desktop + radix `Dialog` drawer on mobile, `TopBar` PanelLeft toggle, `Sidebar` with collapsible Types (links to `/items/[type]`, colored icons, counts, active state) and Collections (Favorites + Recent) sections, bottom user avatar area, `/items/[type]` stub route
- Dashboard UI Phase 3 — Main workspace built out (server components, mock data imported directly): `page.tsx` composes a "Dashboard" header, `StatsSection` (4 `StatCard`s — total items, collections, favorite items, favorite collections), `CollectionsSection` (`CollectionCard` grid with color-coded left accent border + item-type icon strip), and two `ItemsSection` lists sharing `ItemRow` (Pinned items, 10 Recent items sorted by `updatedAt`). Shared `src/lib/type-presentation.ts` for type icon/color maps
- Prisma + Neon PostgreSQL Setup — **Prisma 7** (pinned `7.10`, npm `latest` is the v8 RC). New Rust-free `prisma-client` generator with required `output = "../src/generated/prisma"` (gitignored); datasource `url` removed from `schema.prisma` and moved to root `prisma.config.ts` (`import "dotenv/config"` + typed `env()`). `src/lib/prisma.ts` singleton uses the mandatory `PrismaPg` driver adapter. Schema: `User`/`Account`/`Session`/`VerificationToken` (Auth.js) + `Item`/`ItemType`/`Collection`/`Tag`/`ItemTag`, with FK indexes, `Item(userId, updatedAt)` composite index, `Tag(userId, name)` unique, cascade deletes on user-owned rows + `ItemTag`, `SetNull` on `Item.collectionId`, `Restrict` on `Item.typeId`. Initial migration `20260904034818_init` created with `migrate dev` (never `db push`) and applied to the Neon dev branch. `prisma/seed.ts` (wired via `migrations.seed`) upserts 7 system item types — run with `npx prisma db seed`. `scripts/test-db.ts` (`npm run test:db`) verifies Neon connectivity and prints row counts. `build` script runs `prisma generate && next build`. Follow-ups: run `prisma migrate deploy` on prod deploy; swap dashboard mock-data imports for Prisma queries.
- Seed Data — Added `bcryptjs` dependency (v3, bundled types). Rewrote `prisma/seed.ts` as an idempotent seed per `context/features/seed-spec.md`: upserts the 7 system item types (spec names/icons/colours, stable `type_*` ids), upserts demo user `demo@devstash.io` / `Demo User` (password `12345678` hashed with bcryptjs 12 rounds, `isPro: false`, `emailVerified: now`), then clears and rebuilds that user's collections/items/tags — 5 collections (React Patterns, AI Workflows, DevOps, Terminal Commands, Design Resources) and 18 items (snippets w/ `language`, prompts, `bash`/`yaml` commands + snippet, real-URL links). `scripts/test-db.ts` now fetches and prints the demo user + collections/items and asserts 5 collections / 18 items / 7 system types. Ran `npx prisma db seed`, `npm run test:db`, `npm run build`, `npm run lint` — all pass. Dashboard still reads `src/lib/mock-data.js`; wiring it to Prisma remains a follow-up.
- Dashboard Collections — Wire to Prisma — New `src/lib/db/collections.ts` (`getRecentCollections`) fetches the demo user's 6 most-recently-updated collections directly via Prisma, including each item's type, to compute per-collection item count, the most-used type (for the card's accent border), and the distinct type list (for the icon strip). `CollectionsSection` is now an async server component calling it instead of `mockCollections`; `CollectionCard` takes the real `CollectionWithStats` shape. `type-presentation.ts`'s hex-to-Tailwind palette extended with the seeded system-type colours (they differ from the old mock hexes) and `palette()` now accepts a nullable hex. `dashboard/page.tsx` marked `export const dynamic = "force-dynamic"` so it isn't statically baked in at build time with stale data. Item lists (Pinned/Recent) still read `mockItems` — out of scope, follow-up. `StatsSection`'s collection counts also still read mock data — follow-up. Ran `npm run build`, `npm run lint` — both pass; verified rendered HTML against the live Neon-seeded data.
- Dashboard Items — Wire to Prisma — New `src/lib/db/items.ts` (`getPinnedItems`, `getRecentItems`, `getItemStats`) fetches the demo user's items directly via Prisma, including each item's type and tags. `dashboard/page.tsx` now fetches pinned + recent (10) items itself (in parallel via `Promise.all`) and passes them to `ItemsSection`, replacing the `mockItems` filter/sort; when there are no pinned items the section renders nothing (unchanged existing `ItemsSection` behavior). `ItemRow` now takes the real `ItemWithType` shape — icon and a `CollectionCard`-style left accent border are both derived from the item's type colour via `palette()`; date formatting switched from parsing an ISO date-only string to formatting a real `Date`. Added `getCollectionStats` to `collections.ts`; `StatsSection` is now an async server component sourcing all four stat cards (total/favorite items, total/favorite collections) live via `getItemStats` + `getCollectionStats`, closing the follow-up left by the collections feature. Removed `type-presentation.ts`'s `typeTextColor` helper (mock-data-only lookup, no longer needed now that callers use `palette()` directly) along with its `mockItemTypes` import. Ran `npm run build`, `npm run lint` — both pass; verified rendered HTML against the live Neon-seeded data (18 items / 5 collections stats, type-coloured borders, no Pinned section since the seed data has no pinned items).
- Dashboard Stats & Sidebar — Wire to Prisma — Per `context/features/stats-sidebar-spec.md` (`StatsSection` was already DB-backed from the prior feature). New `getItemTypesWithCounts()` in `src/lib/db/items.ts` fetches system (+ any custom) item types with live per-type item counts, in a fixed display order (snippet, prompt, command, note, file, image, link). `src/lib/db/collections.ts` factored its query/mapping into a shared `fetchCollectionsWithStats` helper and added `getSidebarCollections()` (all of the user's collections, unlike the capped `getRecentCollections`). `Sidebar` now takes `itemTypes`/`collections` as props instead of `mock-data.ts`, sourced by `dashboard/layout.tsx` (marked `force-dynamic`) and threaded through `DashboardShell` to both the desktop and mobile sidebars; it reuses `type-presentation.ts`'s icon/colour palette instead of its own duplicate maps (dropped `TYPE_ICONS`/`TYPE_COLORS`). `type-presentation.ts`'s palette gained a `dot` (solid `bg-*`) variant per colour. Non-favorite ("Recent") collections in the sidebar now show a colored dot keyed to their most-used item type instead of an item count; favorites keep their star. Added a "View all collections" link under the list, pointing to a new `/collections` stub page (mirrors the existing `/items/[type]` placeholder style). `/items/[type]/page.tsx` switched from `mockItemTypes` to `getItemTypesWithCounts()` (dropped `generateStaticParams`, marked `force-dynamic`) so the sidebar's DB-derived links (e.g. `/items/snippet`, singular/lowercase) resolve correctly. Also updated `prisma/seed.ts`: added `isFavorite` to `SeedCollection` and marked React Patterns and Terminal Commands as favorites (the seed previously left every collection non-favorite, so the sidebar's Favorites group had nothing to show) — re-ran `npx prisma db seed`. Ran `npm run build`, `npm run lint` — both pass; verified rendered HTML against the live Neon-seeded data (sidebar type counts sum to 18, 2 favorite + 3 recent collections, `/items/[type]` and `/collections` respond 200, unknown type 404s).
- Add Pro Badge to Sidebar — Per `context/features/add-pro-badge-sidebar.md`. Added the ShadCN `Badge` component (`src/components/ui/badge.tsx`, installed via `npx shadcn add badge`; fixed the CLI's generated import from a nonexistent `cn` package to the project's `@/lib/utils` alias, and removed the stray `cn` dependency it had added to `package.json`). `Sidebar.tsx` now renders a subtle outline "PRO" badge (`text-[10px]`) next to the Files and Images entries in the Types list, matched by a `PRO_TYPE_NAMES` set against `type.name`; hidden when the sidebar is collapsed, same as the item count it sits beside. Ran `npm run build`, `npm run lint` — both pass; verified visually in the browser via Playwright screenshot.
- Codebase Quick-Win Cleanup — Three low-risk fixes from a full-codebase `code-scanner` audit (auth-related findings excluded, since auth isn't implemented yet). Added `@@index([userId, updatedAt])` to `Collection` in `prisma/schema.prisma`, matching `Item`'s existing composite index, applied via a real migration (`prisma migrate dev`, `20260905041214_add_collection_updated_at_index`) so dev/prod stay syncable. New `src/lib/db/user.ts` exports `getDemoUserId`, wrapped in React's `cache()` so the ~7 identical `prisma.user.findUnique` calls per dashboard render collapse into one per request; `items.ts` and `collections.ts` now import it instead of each re-deriving the demo user id (plain Prisma Client calls only, no raw SQL). Added scoped `getItemTypeByName()` (`items.ts`) and `getCollectionCount()` (`collections.ts`) so the `/items/[type]` and `/collections` placeholder pages no longer fetch full datasets just to do a 404 check or print a count. Ran `npm run build`, `npm run lint` — both pass; verified locally that `/dashboard`, `/items/snippet` (200, count 4), `/items/nonexistent` (404), and `/collections` (200, count 5) all behave correctly.
- Auth Phase 1 — NextAuth v5 + GitHub Provider — Per `context/features/auth-phase-1-spec.md`. Installed `next-auth@beta` (`5.0.0-beta.32`) + `@auth/prisma-adapter` (`2.11.3`). Split-config pattern for edge/proxy safety: `src/auth.config.ts` exports the adapter-free slice (`{ providers: [GitHub] } satisfies NextAuthConfig`; GitHub reads `AUTH_GITHUB_ID`/`AUTH_GITHUB_SECRET` from env). `src/auth.ts` is the full instance imported everywhere else — `PrismaAdapter(prisma)` (the `@/lib/prisma` singleton) + `session: { strategy: "jwt" }` + `jwt`/`session` callbacks threading `user.id` onto the token and session, spreading `...authConfig`. `src/app/api/auth/[...nextauth]/route.ts` re-exports `{ GET, POST } = handlers`. `src/proxy.ts` (Next 16's renamed middleware, same level as `app/`) builds its own lightweight `NextAuth(authConfig)` with no adapter and `export const proxy = auth((req) => …)` — redirects unauthenticated requests to Auth.js's built-in `/api/auth/signin` with a `callbackUrl`; `matcher: ["/dashboard", "/dashboard/:path*"]`. `src/types/next-auth.d.ts` augments `Session["user"]` and `JWT` with `id`. No custom `pages.signIn` — uses the default page. Added `AUTH_SECRET`/`AUTH_GITHUB_ID`/`AUTH_GITHUB_SECRET` placeholders + a GitHub callback-URL note to `.env.example` (real values already in `.env`). Verified via Context7 that the v5 split-config + Next 16 `proxy.ts` conventions are current. Ran `npm run build`, `npm run lint` — both pass; browser-tested: `/dashboard` 302s to `/api/auth/signin?callbackUrl=…`, the default sign-in page renders "Sign in with GitHub", and the button hands off to `github.com/login/oauth/authorize` with the correct `client_id` + `…/api/auth/callback/github` redirect URI + PKCE. Did not complete a live GitHub login round-trip (needs interactive credentials). Auth Phase 2 & 3 specs exist in `context/features/` but are not yet implemented.
- Auth Phase 2 — Credentials (Email/Password) Provider — Per `context/features/auth-phase-2-spec.md`. Added email/password sign-in alongside GitHub OAuth using the split-config pattern. `src/auth.config.ts` gains a `Credentials` provider with an `authorize: () => null` placeholder (keeps the edge/proxy bundle free of `bcryptjs`/Prisma). `src/auth.ts` maps over `authConfig.providers`, finds the `credentials` entry by `provider.id`, and replaces it with the real `authorize`: `signInSchema.safeParse` the credentials → `prisma.user.findUnique` by lowercased email → `bcrypt.compare` against `user.password` → return `{ id, name, email, image }` or `null` (existing `jwt`/`session` callbacks thread `user.id` through unchanged). New `POST /api/auth/register` route (`src/app/api/auth/register/route.ts`) — API route not Server Action, per standards: parses JSON (400 on bad body), `registerSchema.safeParse` (400 + `fieldErrors`), duplicate-email check (409, plus a `Prisma.PrismaClientKnownRequestError` `P2002` race fallback), `bcrypt.hash(pw, 12)`, `prisma.user.create`, returns `{ success, data }` 201 / `{ success, error }` on failure. New `src/lib/validations/auth.ts` — shared `signInSchema` (email + non-empty password) and `registerSchema` (trimmed name, lowercased email, 8–72 char password, `.refine()` password-match on `confirmPassword`), plus a `RegisterInput` type. Added `zod` (`^4.5.4`) dependency — `z.email()`, Zod v4 API. `User.password` already existed from the init migration, so no schema change. Verified via Context7 that the Auth.js v5 Credentials + Zod pattern is current. Ran `npm run build`, `npm run lint` — both pass. Curl-tested: register new user → 201; duplicate → 409; password mismatch / short password / non-JSON → 400; sign in via `/api/auth/callback/credentials` with correct password → 302 → `/dashboard` with `authjs.session-token` set and `/api/auth/session` exposing `user.id`; wrong password → 302 back to signin `error=CredentialsSignin`, session `null`; seeded demo user (`demo@devstash.io` / `12345678`) signs in; `/api/auth/providers` lists both `github` + `credentials`; GitHub OAuth POST handoff still 302s to `github.com/login/oauth/authorize` with correct `client_id` + PKCE (no regression); unauthenticated `/dashboard` still redirects to signin with `callbackUrl`. Left a `test@test.com` user in the dev DB from testing (harmless). No registration UI yet — that's Auth Phase 3.
- Auth Phase 3 — Auth UI (Sign In, Register, Sign Out) — Per `context/features/auth-phase-3-spec.md`. Replaced the Auth.js default sign-in page with custom UI and wired the sidebar to the real session. `src/auth.config.ts` gains `pages: { signIn: "/sign-in" }`; `src/proxy.ts` now redirects unauthenticated hits to `/sign-in` (was `/api/auth/signin`) and its `matcher` also covers `/profile`. New pages (all server components that redirect already-authed users via `auth()`): `/sign-in` (`safeCallbackUrl` allows only same-origin relative paths, else `/dashboard`), `/register`, `/profile` (stub — avatar/name/email + "coming soon"). New client components: `src/components/auth/SignInForm.tsx` — email+password via `signIn("credentials", { redirect: false })` with `signInSchema` pre-check + inline error, plus a `signIn("github", { callbackUrl })` button; `src/components/auth/RegisterForm.tsx` — `registerSchema.safeParse` → field errors, `fetch("/api/auth/register", POST)`, `router.push("/sign-in")` on `res.ok`; `src/components/auth/GitHubIcon.tsx` — inlined GitHub mark (lucide-react dropped brand icons). New `src/components/ui/user-avatar.tsx` — `UserAvatar` (radix `Avatar`, GitHub `image` with initials fallback) + exported `initials()` ("Brad Traversy" → "BT", "?" fallback). New `src/components/dashboard/SidebarUser.tsx` — replaces the `mockUser` block at the sidebar bottom: avatar `Link`s to `/profile`, a radix `DropdownMenu` (`side="top"`) with Profile + Sign out; `signOut({ redirect: false })` then `router.push("/sign-in")`. `Sidebar.tsx` dropped the `mockUser`/`Settings` imports and local `initials`, takes a `user` prop, renders `<SidebarUser>`; `DashboardShell.tsx` threads `user` to both (desktop + mobile) `Sidebar` instances; `dashboard/layout.tsx` resolves the session with `auth()` inside its existing `Promise.all` and passes `{ name, email, image }`. Verified via Context7 that the v5 client `signIn`/`signOut` (`redirect:false` → `{ ok, error }`) + custom-pages pattern is current. Ran `npm run build`, `npm run lint` — both pass. Browser-tested (Playwright): unauth `/dashboard` + `/profile` → `/sign-in?callbackUrl=…`; live GitHub session shows the GitHub image in the sidebar; registered `ada@example.com` → redirect to `/sign-in`; email/password sign-in → `/dashboard` with "AL" initials avatar; avatar dropdown (Profile / Sign out); Sign out → `/sign-in`; `/profile` renders. Known follow-ups (from review, not fixed): (1) `proxy.ts` sets `callbackUrl` to the absolute `req.nextUrl.href`, which `safeCallbackUrl` rejects — so post-login always lands on `/dashboard` even from a `/profile` deep link (fix: send `pathname + search`); (2) the GitHub button has no `.catch`, so a rejected `signIn()` leaves it stuck disabled; (3) `mockUser` in `src/lib/mock-data.ts` is now unused. Also left an `ada@example.com` / `analytical1843` user in the dev DB. `.playwright-mcp/` artifacts dir is untracked and not gitignored (pre-existing).
- Email Verification on Register — Inline spec: new email/password accounts must confirm their address (via a Resend link) before they can sign in. Added `resend@^6.26.0`. New `src/lib/email.ts` (Resend client singleton + `sendVerificationEmail(to, url)`, HTML+text, `EMAIL_FROM` env → falls back to `onboarding@resend.dev`), `src/lib/tokens.ts` (`createVerificationToken` — 256-bit hex, drops prior tokens, 24h TTL; `consumeVerificationToken` — single-use + expiry-checked; `latestVerificationToken` + `RESEND_DEBOUNCE_MS` 60s), `src/lib/base-url.ts` (`getBaseUrl(request)` — `AUTH_URL`/`NEXT_PUBLIC_APP_URL` override else forwarded-host). Reuses the existing unused `VerificationToken` table — **no schema change** (`User.emailVerified` already existed). `POST /api/auth/register` now mints a token + sends the email in a try/catch that only logs on failure (still returns 201) and routes the client to `/sign-in?registered=1`. New `GET /api/auth/verify-email?token=…` — consumes the token, `updateMany` sets `emailVerified` (no-op if unknown/already verified), redirects `/sign-in?verified=1` on success / `?error=verification` otherwise. New `POST /api/auth/resend-verification` — always `{ success: true }` 200 (no account enumeration), only sends for a real still-unverified user and only past the 60s debounce; `resendVerificationSchema` added to `src/lib/validations/auth.ts`. `src/auth.ts` `authorize` throws `UnverifiedEmailError extends CredentialsSignin` (`code = "unverified_email"`) when the password is correct but `emailVerified` is null — fires only after a correct password, so no enumeration. `SignInForm.tsx` shows a distinct "verify your email" alert + inline "Resend verification email" action on that code; `sign-in/page.tsx` renders banners for `registered` / `verified` / `error=verification`. GitHub OAuth unaffected (check is credentials-only; OAuth never hits `authorize`). Verified via Context7 that the Auth.js v5 `CredentialsSignin` `code` + Resend Node SDK patterns are current. Ran `npm run build`, `npm run lint` — both pass; full round-trip browser-tested (register → unverified sign-in blocked with distinct message + working resend + 60s debounce → verify link → `emailVerified` set, token consumed, reuse/bogus/missing token all → `?error=verification` → verified user signs in → `/dashboard`); resend/verify edge cases curl-tested. Also added `scripts/prune-users.ts` (`npm run db:prune-users`, dry-run unless `--yes`) — deletes every user except `demo@devstash.io` and their content in one dependency-ordered `$transaction`; ran it, dev DB now has only the demo user (18 items / 5 collections / 7 system types intact). Follow-ups: with `EMAIL_FROM` unset, Resend's test sender only delivers to the Resend account owner's address — real delivery needs a verified domain (`EMAIL_FROM` + `NEXT_PUBLIC_APP_URL`, no code change); resend throttle is per-token DB debounce only, no cross-request rate limiting.
- Email Verification Toggle Flag — Inline spec: one flag to switch the whole email-verification requirement off while no domain is verified in Resend (the shared test sender only reaches the Resend account owner). New `src/lib/auth-flags.ts` — `emailVerificationEnabled()` reads server-side env `EMAIL_VERIFICATION_ENABLED`, returns `true` unless the value is literally `"false"` (case-insensitive); unset ⇒ enabled, so disabling is a deliberate opt-out. Not `NEXT_PUBLIC_` — all four consumers run on the server. Chose an env var over a DB settings row (deploy-time infra toggle, not a runtime/per-user preference) and over a hardcoded const (env flips per-environment with no diff). Wired in: `POST /api/auth/register` — when disabled, creates the user with `emailVerified: new Date()` and skips the `createVerificationToken` + `sendVerificationEmail` block (still 201, same client redirect to `/sign-in?registered=1`); `src/auth.ts` `authorize` — `emailVerificationEnabled() && !user.emailVerified` before throwing `UnverifiedEmailError`, so a correct password logs straight in when off; `POST /api/auth/resend-verification` — early-returns `{ success: true }` 200 after schema validation (malformed body still 400); `sign-in/page.tsx` (server component) — the `?registered=1` banner reads "Your account is ready — sign in below." instead of "Check your email…" when off. `SignInForm.tsx` untouched — its `unverified_email` branch is simply unreachable when off. `verify-email` route untouched — stale links stay idempotent. GitHub OAuth unaffected (never hits `authorize`). `.env.example` documents `EMAIL_VERIFICATION_ENABLED="true"` with the Resend-domain caveat. Note: toggling off does not backfill `emailVerified` on existing unverified rows — `authorize` just stops checking; toggling back on re-blocks them (verified in the browser with a user registered while the flag was on). Ran `npm run build`, `npm run lint` — both pass. Full round-trip browser + curl tested both ways: flag on = unchanged (unverified user blocked with the verify alert + resend, token minted); flag off = register creates a verified account with 0 tokens, both a new user and a pre-existing unverified user sign straight through to `/dashboard`, resend is a no-op 200 (bad body still 400), banner copy softened. Test users pruned via `npm run db:prune-users` — dev DB back to `demo@devstash.io` only (18 items / 5 collections / 7 system types intact).
- Forgot Password — Inline spec: a "forgot password" link + full reset flow for email/password accounts, reusing the `VerificationToken` model for reset tokens (**no schema change**). New `src/lib/tokens.ts` helpers `createPasswordResetToken` / `latestPasswordResetToken` / `consumePasswordResetToken`, all wrapping the existing verification-token helpers with a **namespaced identifier** (`pwreset:<email>`) so a pending reset token and a pending email-verification token for the same address don't clobber each other (`createVerificationToken` deletes every row for an identifier before minting; the `pwreset:` prefix can't occur in a real email). `consumePasswordResetToken` returns `null` if the token's identifier lacks the prefix. Same 24h TTL, single-use, expiry-checked. New `sendPasswordResetEmail(to, resetUrl)` in `src/lib/email.ts` — mirrors `sendVerificationEmail` (HTML+text, `EMAIL_FROM` → `onboarding@resend.dev` fallback, throws on send failure), subject "Reset your DevStash password". `src/lib/validations/auth.ts` gains `forgotPasswordSchema` (`{ email }`, lowercased) and `resetPasswordSchema` (`{ token, password (8–72), confirmPassword }` with the password-match `.refine`, matching `registerSchema`). New `POST /api/auth/forgot-password` (API route, not a Server Action — matches `register`/`resend-verification`): 400 on bad JSON / invalid email; otherwise always `{ success: true }` 200 (no account enumeration); mints a reset token + sends the email **only** when the address maps to a real user with `password` set (GitHub-only accounts silently no-op) and only past the `RESEND_DEBOUNCE_MS` (60s) per-identifier debounce; the Resend send is in a try/catch that logs but doesn't fail the response. New `POST /api/auth/reset-password`: `resetPasswordSchema` 400 (with `fieldErrors`) / bad JSON 400; `consumePasswordResetToken` → generic "invalid or expired" 400 on failure; on success `bcrypt.hash(pw, 12)` + `prisma.user.updateMany` sets the password, then a second scoped `updateMany` stamps `emailVerified` on the row **if still null** (receiving the emailed link proves ownership); `{ success: true }` 200. New pages (server components that redirect already-authed users via `auth()`, matching `/sign-in` & `/register`): `/forgot-password` → `ForgotPasswordForm` (single email input; always shows the same "if an account exists, we've sent a link" confirmation, even on network error, to avoid leaking); `/reset-password` reads `?token=` and renders `ResetPasswordForm` (new+confirm password → `POST /api/auth/reset-password` → `router.push("/sign-in?reset=1")`; token-error state links back to `/forgot-password`), or a "missing token" state when the param is absent. `SignInForm.tsx` — "Forgot password?" link (`text-xs`, muted) in a flex row beside the Password label. `sign-in/page.tsx` — new `?reset=1` info banner ("Password updated — sign in with your new password."), ordered before the existing `verified`/`registered`/`error` branches. No `proxy.ts` change (both pages are public). Verified against the existing auth/token/email patterns (no new deps, no Context7 needed). Ran `npm run build`, `npm run lint` — both pass. Full round-trip browser-tested (sign-in link → forgot form → confirmation → reset form with a real token → `/sign-in?reset=1` banner → sign in with the new password → `/dashboard`; old password rejected). Curl-tested: bad JSON / invalid email → 400; unknown email → 200 no token; demo user → 200, token minted as `pwreset:demo@devstash.io`; 2nd request within 60s → 200, no new token (debounce); bogus / reused token → 400; short / mismatched password → 400. Demo password restored via `npx prisma db seed` — dev DB back to `demo@devstash.io` only (18 items / 5 collections / 7 system types, 0 tokens). Follow-ups: (1) password reset shares the Resend verified-domain caveat — with `EMAIL_FROM` unset the test sender only reaches the Resend account owner; unlike verification there's no disable flag (reset inherently needs email); (2) `forgot-password` awaits the send only for real password users, a minor timing side-channel identical to the existing `resend-verification` route; (3) passing a verification token to `/api/auth/reset-password` consumes (deletes) it before the prefix check rejects it — no practical risk (256-bit unguessable tokens), symmetric with `verify-email`; (4) GitHub-only no-op path not live-tested (no OAuth-only user in the dev DB), but it's a plain `user?.password` truthy check covered by the unknown-email case.
- Rate Limiting for Auth — Per `context/features/rate-limiting-spec.md`. Sliding-window rate limiting on the auth API routes (brute force / credential stuffing / mail-relay abuse). Added `@upstash/ratelimit` + `@upstash/redis`. New `src/lib/rate-limit.ts`: lazy `Redis` singleton from `UPSTASH_REDIS_REST_URL`/`UPSTASH_REDIS_REST_TOKEN` (both unset ⇒ limiting **disabled**, logs a warning, `checkRateLimit` returns `{ success: true }`); a `Map`-memoised `Ratelimit` per name+limit+window (`slidingWindow`, `prefix: "ratelimit:<name>"`, `timeout: 1000` so a slow Redis fails open, `analytics: false`). `getClientIp(request)` = first `x-forwarded-for` entry → `x-real-ip` → `"127.0.0.1"`. `checkRateLimit({ request, name, limit, window, identifier? })` keys `ip` or `ip:email.toLowerCase()`, wraps `limiter.limit()` in try/catch → **fails open** `{ success: true, remaining: limit, reset: 0 }` on any throw. `rateLimitResponse(reset)` → `429` `{ error: "Too many attempts. Please try again in X minutes." }` (`Math.ceil` mins, floored at 1) + `Retry-After` header (seconds). `credentialsRateLimitResponse(request, reset)` → same status/header but body `{ url: "<origin>/sign-in?error=CredentialsSignin&code=rate_limited", error }` because the client `signIn()` does `new URL(data.url)` unconditionally. Wired in (all after JSON-parse + schema-validation, before the real work): `register` 3/1h by IP; `forgot-password` 3/1h by IP; `reset-password` 5/15m by IP; `resend-verification` 3/15m by IP+email (before the `emailVerificationEnabled()` early-return); login via a **POST wrapper** in `src/app/api/auth/[...nextauth]/route.ts` — `export const { GET } = handlers` unchanged, but `POST` is now a function that, when `pathname.endsWith("/callback/credentials")`, reads `request.clone().formData()` for the email (falls back to IP-only if unreadable), calls `checkRateLimit` (5/15m, `name: "auth:login"`, IP+email), returns `credentialsRateLimitResponse` on limit, else `handlers.POST(request)`. Frontend (inline, per the existing `role="alert"` pattern — no toast lib in the project): `SignInForm.tsx` adds a `result?.code === "rate_limited"` branch → "Too many sign-in attempts. Please wait a few minutes and try again."; `ForgotPasswordForm.tsx` captures the response and on `status === 429` shows `body.error` inline instead of flipping to its always-shown confirmation screen. `RegisterForm`/`ResetPasswordForm` already render `body.error` on non-ok, so unchanged. Incidental: `src/lib/email.ts` `new Resend(apiKey ?? "re_missing_api_key")` → `apiKey || …` so an empty-string `RESEND_API_KEY` (present in the untracked `.env.production`) stops throwing during `next build` — a pre-existing breakage unrelated to this feature. `.env.example` documents `UPSTASH_REDIS_REST_URL`/`_TOKEN` (real values already in `.env` + `.env.production` — user pre-provisioned an Upstash DB). `npm run lint` + `npm run build` pass. Live-tested against real Upstash (dev): each protected route passes up to its limit then `429` + `Retry-After: 480` with the right JSON body; login — 5 wrong-password attempts reach NextAuth (`302 CredentialsSignin`), 6th → `429 code=rate_limited`, **correct password still logs in** (`302 → /dashboard`, session cookie set); bad JSON still `400` (not consumed by the limiter); fail-open verified by pointing at an unreachable Upstash host (all requests still succeeded, ~1.5s each via the timeout). Browser (Playwright): SignInForm shows the rate-limit message after the limit; ForgotPasswordForm shows "Too many attempts. Please try again in 5 minutes." inline. Test users pruned (`npm run db:prune-users --yes`) — dev DB back to `demo@devstash.io` only (18 items / 5 collections / 7 system types; demo password untouched). Follow-ups: (1) with a `slidingWindow`, `reset` is the next window's start, so "try again in X minutes" is a lower bound and the effective cutover can be ~1 request early near a window boundary (observed 4-through in one browser run vs 5 via curl) — fine for security limiting; (2) `timeout: 1000` adds ~1s latency per request during a Redis outage; (3) still no cross-request rate limiting story for the authenticated routes (`change-password`, `delete-account`) — out of spec scope; (4) the `email.ts` `||` fix is the minimal unblock — the real prod gap is `RESEND_API_KEY=""` in `.env.production`.
- GitHub Sign-In via Server Action — Inline spec: the client-side `signIn("github", { callbackUrl })` from `next-auth/react` in `SignInForm.tsx` wasn't producing a usable OAuth redirect, so move the GitHub handoff server-side. New `src/actions/auth.ts` (`"use server"`) exports `signInWithGitHub(redirectTo: string)` → `await signIn("github", { redirectTo })` importing `signIn` from `@/auth` (the full instance); `signIn` redirects by throwing (`NEXT_REDIRECT`), so the action never returns on success and Auth.js issues a real HTTP redirect to `github.com/login/oauth/authorize`. `SignInForm.tsx` imports `signInWithGitHub` from `@/actions/auth`; the GitHub button's `onClick` now does `setPending("github"); void signInWithGitHub(callbackUrl)` instead of the client `signIn("github", { callbackUrl })`. The `next-auth/react` `signIn` import stays — the email/password path still uses `signIn("credentials", { redirect: false })` on the client for its inline error handling. First file under `src/actions/` (matches the `src/actions/[feature].ts` standard). No new deps, no schema change, no `proxy.ts` change. Ran `npm run lint`, `npm run build` — both pass. Follow-up: not live-tested through a full GitHub OAuth round-trip (needs interactive credentials); the pre-existing stuck-disabled-on-rejection behavior of the GitHub button is unchanged (a redirect throw isn't a rejection, so `setPending("github")` staying set until navigation is expected).
- Profile Page — Per `context/features/profile-spec.md`. Replaced the `/profile` "coming soon" stub with a real profile page + account actions. New `src/lib/db/profile.ts`: `getProfileUser(userId)` — **session-scoped** (takes the signed-in user's id, unlike the rest of `src/lib/db/*` which is hardcoded to the demo user via `getDemoUserId`), selects name/email/image/`emailVerified`/`isPro`/`createdAt` and derives a `hasPassword` boolean (`password !== null`, never returns the hash); `getProfileStats(userId)` — `Promise.all` of item count, collection count, item types (`OR: [{ isSystem }, { userId }]`), and an `item.groupBy({ by: ["typeId"] })`, mapped to a `{ id,name,icon,color,count }[]` sorted by the type order. `src/lib/db/items.ts`: `const TYPE_ORDER` → `export const TYPE_ORDER`, plus a new exported `compareTypeOrder(a,b)` comparator (system order then alpha) factored out of `getItemTypesWithCounts`'s inline `.sort` and reused by profile stats. `src/app/profile/page.tsx` (`force-dynamic`, `auth()`-guarded, also `redirect`s if the session's user row is gone): back-to-dashboard link, `UserAvatar` (GitHub image else initials) + name + email, a badge row (`Calendar` "Joined <long date>", "Free/Pro plan", `BadgeCheck` "Email verified" when `emailVerified`), `<ProfileStats>`, and an Account section — a Password card rendered **only when `user.hasPassword`** (so GitHub-OAuth-only accounts don't see it) wrapping `<ChangePasswordForm>`, and a destructive-bordered Delete-account card wrapping `<DeleteAccountDialog>`. New `src/components/profile/`: `ProfileStats.tsx` (presentational — two `StatCard`s for item/collection totals + a 2/3-col grid of type chips using `TYPE_ICON`/`palette().text` from `type-presentation.ts`); `ChangePasswordForm.tsx` (client, collapsible — button reveals the form; `changePasswordSchema.safeParse` → inline field errors, `POST /api/auth/change-password`, on ok `formEl.reset()` + "Password updated." + re-collapse; `if (pending) return` guard against double-submit; a hidden `autocomplete="username"` input carrying the email so browsers pair the change and Chrome's SPA-submit heuristics stay quiet); `DeleteAccountDialog.tsx` (client, radix `Dialog` mirroring `DashboardShell`'s overlay/animation classes — the confirm button stays `disabled` until the typed value equals the account email case-insensitively; on success `signOut({ redirect: false })` → `router.push("/sign-in")` → `router.refresh()`). New API routes (both `await auth()`-guarded → 401 when unauthenticated, JSON-body-guarded → 400): `POST /api/auth/change-password` — `changePasswordSchema` (400 + `fieldErrors`), `prisma.user.findUnique` → `if (!user?.password)` 400 ("doesn't use a password"), `bcrypt.compare` current password → 400 ("incorrect"), `bcrypt.hash(new, 12)` + `user.update`, `{ success: true }`; `POST /api/auth/delete-account` — `deleteAccountSchema` (`{ confirmation }`), loads `{ id, email }` (missing ⇒ `{ success: true }`, session outlived the row), `confirmation.trim().toLowerCase() !== email.toLowerCase()` → 400, then `prisma.user.delete({ where: { id } })` — the init migration's FK `ON DELETE CASCADE` on `Item`/`Collection`/`Tag`/`ItemType`/`Account`/`Session`.userId (and `ItemTag` from `Item`) clears all owned rows in one statement — followed by a `verificationToken.deleteMany` for `identifier IN (email, "pwreset:"+email)` since that table has no relation. `src/lib/validations/auth.ts`: `changePasswordSchema` (currentPassword non-empty, newPassword 8–72, confirm; `.refine` match on `confirmPassword` + `.refine` newPassword ≠ currentPassword on `newPassword`) and `deleteAccountSchema` (`{ confirmation: min(1) }`). No schema change, no new deps, no `proxy.ts` change (matcher already covers `/profile`). Ran `npm run build`, `npm run lint` — both pass. Browser-tested (Playwright, demo user): profile renders — 18 items / 5 collections, type breakdown snippet 4 / prompt 3 / command 5 / note 0 / file 0 / image 0 / link 6 (= 18), badges correct; change password success → "Password updated." + collapse; wrong current password → inline "Current password is incorrect." with the form kept open; delete-account confirm button gated on the exact email; ran a **real** end-to-end deletion — `POST /api/auth/delete-account` 200, demo user + all 18 items / 5 collections cascade-deleted, the 7 system item types (`userId = null`) untouched, `signOut` → `/sign-in`. Curl: unauth `/profile` → 302 `/sign-in?callbackUrl=…`, unauth `POST` to both routes → 401. Dev DB restored with `npx prisma db seed` (1 user / 18 items / 5 collections / 7 types; demo user id changed as a result). Follow-ups: (1) the GitHub-OAuth-only path (Password card hidden, `change-password` 400) is logic-only — no OAuth-only user in the dev DB to click through; (2) shares the standing Resend verified-domain caveat only indirectly (no email is sent by this feature); (3) `ProfileStats` counts come from a separate `groupBy` than the dashboard's `getItemTypesWithCounts`, so the two share the comparator but not the query.
