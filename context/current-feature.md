# Current Feature: AI Auto-Tagging

## Status

In Progress

## Goals

- Establish the OpenAI foundation (first AI feature): client utility with an `AI_MODEL` constant using the standard `openai` SDK, kept simple
- Add an AI rate limit config (20 requests/hour per user) to the existing `src/lib/rate-limit.ts` utility
- Create a `generateAutoTags` server action with auth check, Pro gating, Zod validation, and rate limiting
- Use the OpenAI **Responses API** (`client.responses.create`), NOT Chat Completions — gpt-5-nano returns empty content on Chat Completions
- Request `text: { format: { type: 'json_object' } }`, read `response.output_text`, parse manually; handle both `{"tags": [...]}` and `[...]` response shapes; normalize tags to lowercase
- Truncate item content to 2000 chars before the API call; base suggestions on title + content
- Add a "Suggest Tags" button (Sparkles icon, ghost variant) near the tags input in both the create item dialog and the item drawer edit mode
- Return 3-5 freeform tag suggestions (not limited to existing DB tags), rendered as badges with per-tag accept (check) / reject (X) controls; accepted tags append to the item's tag list
- Pro-only: hide the button for free users (UI gating via an `isPro` prop / client fetch) AND enforce server-side in the action
- Error handling via `sonner` toast for Pro gating, rate limit, and AI service errors
- Follow existing patterns (server action shape, rate-limit helpers, validation module layout)
- Unit tests for the `generateAutoTags` server action

## Notes

- First AI feature — no OpenAI client, no AI rate-limit config exists yet; this feature creates them
- Model: `gpt-5-nano` via `AI_MODEL` constant; `OPENAI_API_KEY` already in `.env`
- `openai` npm v6+ has two APIs — must use Responses API. `zodResponseFormat` structured output blows the token budget with this model; use `json_object` + manual parse
- **Gotcha (fixed):** with `text.format: json_object` the Responses API 400s unless the literal word "json" appears in the `input` itself (not just `instructions`) — `auto-tags.ts` appends `Return the tags as a JSON object…` to the input
- **Also fixed (adjacent, user-reported):** stale `confirmingDelete` could carry into the next item opened after a delete — `ItemDrawer` now also resets transient state while `!open`, and `useItemDrawer.handleDeleted` clears `summary`/`detail`
- `max_tokens` is unsupported by gpt-5-nano
- `isPro` is available server-side via session but is not currently passed to the create/edit UI components — UI gating requires threading `isPro` as a prop or fetching it client-side (server-side gating is the enforcement)
- Full architectural context: `docs/ai-integration-plan.md`
- Spec: `context/features/ai-auto-tag-spec.md`

## History

- Project setup and boilerplate cleanup
- Initial Next.js and Tailwind setup committed and pushed to GitHub
- Dashboard UI Phase 1 — ShadCN init (radix-nova preset) + button/input components, `/dashboard` route with layout shell, dark mode by default, display-only top bar (search + New Collection/New Item), placeholder sidebar & main area
- Dashboard UI Phase 2 — Functional sidebar: `DashboardShell` client wrapper owning sidebar state, collapsible `w-64`/`w-16` rail on desktop + radix `Dialog` drawer on mobile, `TopBar` PanelLeft toggle, `Sidebar` with collapsible Types (links to `/items/[type]`, colored icons, counts, active state) and Collections (Favorites + Recent) sections, bottom user avatar area, `/items/[type]` stub route
- Dashboard UI Phase 3 — Main workspace built out (server components, mock data imported directly): `page.tsx` composes a "Dashboard" header, `StatsSection` (4 `StatCard`s — total items, collections, favorite items, favorite collections), `CollectionsSection` (`CollectionCard` grid with color-coded left accent border + item-type icon strip), and two `ItemsSection` lists sharing `ItemRow` (Pinned items, 10 Recent items sorted by `updatedAt`). Shared `src/lib/type-presentation.ts` for type icon/color maps
- Prisma + Neon PostgreSQL Setup — **Prisma 7** (pinned `7.10`, npm `latest` is the v8 RC). New Rust-free `prisma-client` generator with required `output = "../src/generated/prisma"` (gitignored); datasource `url` removed from `schema.prisma` and moved to root `prisma.config.ts` (`import "dotenv/config"` + typed `env()`). `src/lib/prisma.ts` singleton uses the mandatory `PrismaPg` driver adapter. Schema: `User`/`Account`/`Session`/`VerificationToken` (Auth.js) + `Item`/`ItemType`/`Collection`/`Tag`/`ItemTag`, with FK indexes, `Item(userId, updatedAt)` composite index, `Tag(userId, name)` unique, cascade deletes on user-owned rows + `ItemTag`, `SetNull` on `Item.collectionId`, `Restrict` on `Item.typeId`. Initial migration `20260904034818_init` created with `migrate dev` (never `db push`) and applied to the Neon dev branch. `prisma/seed.ts` (wired via `migrations.seed`) upserts 7 system item types — run with `npx prisma db seed`. `scripts/test-db.ts` (`npm run test:db`) verifies Neon connectivity and prints row counts. `build` script runs `prisma generate && next build`. Follow-ups: run `prisma migrate deploy` on prod deploy; swap dashboard mock-data imports for Prisma queries.
- Seed Data — Added `bcryptjs` dependency (v3, bundled types). Rewrote `prisma/seed.ts` as an idempotent seed per `context/features/seed-spec.md`: upserts the 7 system item types (spec names/icons/colours, stable `type_*` ids), upserts demo user `demo@devstash.io` / `Demo User` (password `12345678` hashed with bcryptjs 12 rounds, `isPro: false`, `emailVerified: now`), then clears and rebuilds that user's collections/items/tags — 5 collections (React Patterns, AI Workflows, DevOps, Terminal Commands, Design Resources) and 18 items (snippets w/ `language`, prompts, `bash`/`yaml` commands + snippet, real-URL links). `scripts/test-db.ts` now fetches and prints the demo user + collections/items and asserts 5 collections / 18 items / 7 system types. Ran `npx prisma db seed`, `npm run test:db`, `npm run build`, `npm run lint` — all pass. Dashboard still reads `src/lib/mock-data.js`; wiring it to Prisma remains a follow-up.
- Dashboard Collections — Wire to Prisma — New `src/lib/db/collections.ts` (`getRecentCollections`) fetches the demo user's 6 most-recently-updated collections directly via Prisma, including each item's type, to compute per-collection item count, the most-used type (for the card's accent border), and the distinct type list (for the icon strip). `CollectionsSection` is now an async server component calling it instead of `mockCollections`; `CollectionCard` takes the real `CollectionWithStats` shape. `type-presentation.ts`'s hex-to-Tailwind palette extended with the seeded system-type colours (they differ from the old mock hexes) and `palette()` now accepts a nullable hex. `dashboard/page.tsx` marked `export const dynamic = "force-dynamic"` so it isn't statically baked in at build time with stale data. Item lists (Pinned/Recent) still read `mockItems` — out of scope, follow-up. `StatsSection`'s collection counts also still read mock data — follow-up. Ran `npm run build`, `npm run lint` — both pass; verified rendered HTML against the live Neon-seeded data.
- Dashboard Items — Wire to Prisma — New `src/lib/db/items.ts` (`getPinnedItems`, `getRecentItems`, `getItemStats`) fetches the demo user's items directly via Prisma, including each item's type and tags. `dashboard/page.tsx` now fetches pinned + recent (10) items itself (in parallel via `Promise.all`) and passes them to `ItemsSection`, replacing the `mockItems` filter/sort; when there are no pinned items the section renders nothing (unchanged existing `ItemsSection` behavior). `ItemRow` now takes the real `ItemWithType` shape — icon and a `CollectionCard`-style left accent border are both derived from the item's type colour via `palette()`; date formatting switched from parsing an ISO date-only string to formatting a real `Date`. Added `getCollectionStats` to `collections.ts`; `StatsSection` is now an async server component sourcing all four stat cards (total/favorite items, total/favorite collections) live via `getItemStats` + `getCollectionStats`, closing the follow-up left by the collections feature. Removed `type-presentation.ts`'s `typeTextColor` helper (mock-data-only lookup, no longer needed now that callers use `palette()` directly) along with its `mockItemTypes` import. Ran `npm run build`, `npm run lint` — both pass; verified rendered HTML against the live Neon-seeded data (18 items / 5 collections stats, type-coloured borders, no Pinned section since the seed data has no pinned items).
- Dashboard Stats & Sidebar — Wire to Prisma — Per `context/features/stats-sidebar-spec.md` (`StatsSection` was already DB-backed from the prior feature). New `getItemTypesWithCounts()` in `src/lib/db/items.ts` fetches system (+ any custom) item types with live per-type item counts, in a fixed display order (snippet, prompt, command, note, file, image, link). `src/lib/db/collections.ts` factored its query/mapping into a shared `fetchCollectionsWithStats` helper and added `getSidebarCollections()` (all of the user's collections, unlike the capped `getRecentCollections`). `Sidebar` now takes `itemTypes`/`collections` as props instead of `mock-data.ts`, sourced by `dashboard/layout.tsx` (marked `force-dynamic`) and threaded through `DashboardShell` to both the desktop and mobile sidebars; it reuses `type-presentation.ts`'s icon/colour palette instead of its own duplicate maps (dropped `TYPE_ICONS`/`TYPE_COLORS`). `type-presentation.ts`'s palette gained a `dot` (solid `bg-*`) variant per colour. Non-favorite ("Recent") collections in the sidebar now show a colored dot keyed to their most-used item type instead of an item count; favorites keep their star. Added a "View all collections" link under the list, pointing to a new `/collections` stub page (mirrors the existing `/items/[type]` placeholder style). `/items/[type]/page.tsx` switched from `mockItemTypes` to `getItemTypesWithCounts()` (dropped `generateStaticParams`, marked `force-dynamic`) so the sidebar's DB-derived links (e.g. `/items/snippet`, singular/lowercase) resolve correctly. Also updated `prisma/seed.ts`: added `isFavorite` to `SeedCollection` and marked React Patterns and Terminal Commands as favorites (the seed previously left every collection non-favorite, so the sidebar's Favorites group had nothing to show) — re-ran `npx prisma db seed`. Ran `npm run build`, `npm run lint` — both pass; verified rendered HTML against the live Neon-seeded data (sidebar type counts sum to 18, 2 favorite + 3 recent collections, `/items/[type]` and `/collections` respond 200, unknown type 404s).
- Add Pro Badge to Sidebar — Per `context/features/add-pro-badge-sidebar.md`. Added the ShadCN `Badge` component (`src/components/ui/badge.tsx`, installed via `npx shadcn add badge`; fixed the CLI's generated import from a nonexistent `cn` package to the project's `@/lib/utils` alias, and removed the stray `cn` dependency it had added to `package.json`). `Sidebar.tsx` now renders a subtle outline "PRO" badge (`text-[10px]`) next to the Files and Images entries in the Types list, matched by a `PRO_TYPE_NAMES` set against `type.name`; hidden when the sidebar is collapsed, same as the item count it sits beside. Ran `npm run build`, `npm run lint` — both pass; verified visually in the browser via Playwright screenshot.
- Codebase Quick-Win Cleanup — Three low-risk fixes from a full-codebase `code-scanner` audit (auth-related findings excluded, since auth isn't implemented yet). Added `@@index([userId, updatedAt])` to `Collection` in `prisma/schema.prisma`, matching `Item`'s existing composite index, applied via a real migration (`prisma migrate dev`, `20260905041214_add_collection_updated_at_index`) so dev/prod stay syncable. New `src/lib/db/user.ts` exports `getDemoUserId`, wrapped in React's `cache()` so the ~7 identical `prisma.user.findUnique` calls per dashboard render collapse into one per request; `items.ts` and `collections.ts` now import it instead of each re-deriving the demo user id (plain Prisma Client calls only, no raw SQL). Added scoped `getItemTypeByName()` (`items.ts`) and `getCollectionCount()` (`collections.ts`) so the `/items/[type]` and `/collections` placeholder pages no longer fetch full datasets just to do a 404 check or print a count. Ran `npm run build`, `npm run lint` — both pass; verified locally that `/dashboard`, `/items/snippet` (200, count 4), `/items/nonexistent` (404), and `/collections` (200, count 5) all behave correctly.
- Auth Phase 1 — NextAuth v5 + GitHub Provider — Per `context/features/auth-phase-1-spec.md`. Installed `next-auth@beta` + `@auth/prisma-adapter`. Split-config: `src/auth.config.ts` (adapter-free slice, GitHub provider) + `src/auth.ts` (full instance, PrismaAdapter, JWT sessions, `jwt`/`session` callbacks threading `user.id`). API route re-exports `handlers`. `src/proxy.ts` (Next 16 middleware) guards `/dashboard`. `src/types/next-auth.d.ts` augments `Session`/`JWT` with `id`.
- Auth Phase 2 — Credentials provider (`authorize` in `src/auth.ts`: Zod parse → `prisma.user.findUnique` → `bcrypt.compare`), `POST /api/auth/register` (API route, Zod, bcrypt.hash 12, 201/409/400). Shared `src/lib/validations/auth.ts`. Added `zod` v4.
- Auth Phase 3 — Auth UI (custom `/sign-in`, `/register`, `/profile` stub; `SignInForm`/`RegisterForm`/`GitHubIcon`; `UserAvatar` + `initials()`; `SidebarUser` with sign-out dropdown; `pages.signIn = "/sign-in"`).
- Email Verification on Register — Resend + `VerificationToken` table (no schema change); `sendVerificationEmail`, token helpers, `getBaseUrl`; register mints+sends, `GET /api/auth/verify-email`, `POST /api/auth/resend-verification` (60s debounce, no enumeration); `authorize` throws `UnverifiedEmailError` on correct-password-but-unverified.
- Email Verification Toggle Flag — `src/lib/auth-flags.ts` `emailVerificationEnabled()` (env `EMAIL_VERIFICATION_ENABLED`, off only when literally `"false"`), wired into register / `authorize` / resend / sign-in banner copy.
- Forgot Password — `pwreset:<email>`-namespaced reuse of `VerificationToken` (no schema change); `create/latest/consumePasswordResetToken`, `sendPasswordResetEmail`; `POST /api/auth/forgot-password` (no enumeration, 60s debounce) + `POST /api/auth/reset-password` (consume → bcrypt.hash → `updateMany`, stamps `emailVerified` if null); `/forgot-password` + `/reset-password` pages + forms; `?reset=1` sign-in banner.
- Rate Limiting for Auth — `@upstash/ratelimit` + `@upstash/redis`. `src/lib/rate-limit.ts` (lazy Redis singleton, disabled+warn when env unset, per-name memoised sliding-window limiter, `getClientIp`, fail-open `checkRateLimit`, `rateLimitResponse`/`credentialsRateLimitResponse`). Wired into register (3/1h IP), forgot-password (3/1h IP), reset-password (5/15m IP), resend-verification (3/15m IP+email), and login via a POST wrapper in the `[...nextauth]` route (5/15m IP+email). Inline `role="alert"` frontend messages.
- GitHub Sign-In via Server Action — New `src/actions/auth.ts` (`"use server"`) `signInWithGitHub(redirectTo)` → `signIn("github", { redirectTo })` from `@/auth`; `SignInForm` GitHub button calls it instead of the client `signIn`. First file under `src/actions/`.
- Profile Page — `src/lib/db/profile.ts` (`getProfileUser` session-scoped, `hasPassword` derived, never returns the hash; `getProfileStats`). `TYPE_ORDER` exported + `compareTypeOrder`. Real `/profile` page (avatar, badges, `ProfileStats`, Account section). `ChangePasswordForm` + `DeleteAccountDialog` (radix `Dialog`). `POST /api/auth/change-password` + `POST /api/auth/delete-account` (auth-gated, cascade delete + `verificationToken.deleteMany`). `changePasswordSchema` + `deleteAccountSchema`.
- Items List View — `getItemsByType(typeId)` in `src/lib/db/items.ts`; `src/components/dashboard/ItemCard.tsx` grid card; `/items/[type]/page.tsx` renders a real type-filtered grid + empty state + live count.
- Vitest Unit Testing Setup — `vitest@^5`, `vitest.config.mts` (node env, `src/{actions,lib}/**/*.test.ts`, `@`→`./src` alias). `test` / `test:watch` scripts. 8 test files / 37 tests. Docs updated (`CLAUDE.md`, `context/ai-interaction.md`, `context/coding-standards.md`).
- Item Listing — Three-Column Grid — `/items/[type]/page.tsx` grid `sm:grid-cols-2 lg:grid-cols-3`.
- Item Drawer — Right-side slide-in `Sheet` item detail view. `src/components/ui/sheet.tsx` (on unified `radix-ui` Dialog). `getItemDetail(id)` + `GET /api/items/[id]`. `src/components/items/ItemBrowser.tsx` (client wrapper, drawer state, fetch + cache) + `src/components/items/ItemDrawer.tsx`. `ItemsSection` + `items/[type]/page.tsx` delegate to `<ItemBrowser>`.
- Item Drawer — Edit Mode — `src/lib/validations/item.ts` (`updateItemSchema`, Zod v4) + tests. `updateItem(id, data)` in `src/lib/db/items.ts` (demo-user-scoped, ownership guard, wholesale tag replace). `src/actions/items.ts` (`"use server"` `updateItem` action) + tests. `ItemDrawer` gains `editing` state + `<ItemEditForm>` (type-specific fields, `sonner` toasts, inline `fieldErrors`). `ItemBrowser` `handleSaved` updates cache + `router.refresh()`. New dep `sonner@^2` + `src/components/ui/sonner.tsx` mounted in `src/app/layout.tsx`.
- Item Delete — `deleteItem(id)` + `deleteItem` server action; `AlertDialog` primitive; drawer trash button → confirm → toast → `onDeleted` evicts cache + refreshes.
- Item Create — `NewItemDialog` (pill type selector, type-conditional fields), `createItemSchema` + `createItem` action + `createItem` query, `TopBar` owns `newItemOpen`.
- Code Editor (Monaco), Markdown Editor, File & Image Upload (R2), Image Gallery View, File List View, Card Quick-Copy Icon, Low-Risk Fixes Batch, Code Breakup refactor — see git history.
- Collection Create — Top-bar "New Collection" opens a create modal. `createCollectionSchema` + `createCollection` query + `POST /api/collections` (API route, not a server action — deliberate divergence from item-create). `NewCollectionDialog` modeled on `NewItemDialog`. Tests: `collection.test.ts` (4) + `collections.test.ts` (3).
- Item ↔ Collections — Many-to-Many + Form Picker — `CollectionItem` join model, `getCollectionOptions` + `GET /api/collections`, `CollectionPicker` in the item forms, migration `20260906120000_item_collections_many_to_many`.
- Collections Pages — List & Detail — real `/collections` list + new `/collections/[id]` detail (`getCollectionById` + `getItemsByCollection`, `<ItemBrowser layout="grid">`). `CollectionCard` became a `<Link>`; sidebar collection rows + "View all" became links.
- Collection Edit / Delete (+ Favorite placeholder) — Per `context/current-feature.md`. **Data layer** (`src/lib/db/collections.ts`): `updateCollection(id, data)` (demo-user scoped; ownership folded into a `updateMany` `where`, `count === 0` → `null`, else re-reads the fresh `CollectionSummary`) and `deleteCollection(id)` (`deleteMany` `where: { id, userId }`, returns `count > 0`). Deleting a collection does **not** touch its items — only the `CollectionItem` join rows cascade away (`onDelete: Cascade`, already in schema). **Validation** (`src/lib/validations/collection.ts`): `updateCollectionSchema` = alias of `createCollectionSchema` (+ `UpdateCollectionInput`). **API** — new `src/app/api/collections/[id]/route.ts` with `PATCH` (auth → `readJsonBody`/`INVALID_JSON` guard → `updateCollectionSchema.safeParse` → `validationErrorResponse` → `updateCollection`, `null` → 404, else `{ success, data }`) and `DELETE` (auth → `deleteCollection`, `false` → 404, else `{ success, data: { id } }`); same `{ success, error, details? }` shape + `src/lib/api/request.ts` helpers as `POST /api/collections` (kept an API route, not a server action, mirroring the create-collection decision). **UI primitive** — new `src/components/ui/dropdown-menu.tsx`, hand-written on `radix-ui`'s `DropdownMenu` (Root/Trigger/Content/Item/Separator; `Item` has a `destructive` variant), same pattern as `dialog.tsx` / `alert-dialog.tsx`; uses `bg-popover` tokens. **Components**: `EditCollectionDialog` (`"use client"`, modeled on `NewCollectionDialog` — shared `Dialog` primitive + `Field` + `textareaClass`, render-phase re-seed from props on open, `postJson(url, body, "PATCH")`, inline `fieldErrors` from `details`, `sonner` toast, `router.refresh()`, optional `onSaved`); `DeleteCollectionDialog` (`AlertDialog` confirmation, `postJson(url, undefined, "DELETE")`, treats a **404 as already-done** so a stale tab still proceeds, then calls `onDeleted` — it does **not** flip its own `open`, so there's no close-vs-navigate race); `CollectionActionsMenu` (the three-dots dropdown for cards — Edit / Favorite / Delete; `onDeleted` = `setDeleteOpen(false)` + `router.refresh()`); `CollectionDetailActions` (Favorite / Edit / Delete icon buttons for the `/collections/[id]` header; `onDeleted` = `setDeleteOpen(false)` + `router.push("/collections")`). **Favorite is a placeholder everywhere — an inert icon button / menu item, no handler** (deferred to a later feature). **`CollectionCard`** restructured from a single `<Link>` to the codebase's stretched-link pattern: a `.group.relative` wrapper holding an `absolute inset-0` `<Link>` overlay (aria-label "Open <name>") under `pointer-events-none relative z-10` content, with `<CollectionActionsMenu>` as an `absolute top-3 right-3 z-10` sibling (its own `<button>` trigger stays clickable); title row got `pr-8` to clear the button. One change covers the dashboard `CollectionsSection` and the `/collections` list. **`/collections/[id]/page.tsx`** — header wrapped in a `flex items-start justify-between` row with `<CollectionDetailActions collection={collection}>` (the existing `getCollectionById` `CollectionSummary` already matches the needed `{ id, name, description, isFavorite }`). **`src/lib/post-json.ts`** — `postJson` gained an optional 3rd `method` arg (default `"POST"`) so it can also do PATCH/DELETE; existing callers unaffected. First cut had `DeleteCollectionDialog` flip its own `open` then call `onDeleted()`, which raced with the AlertDialog close and sometimes swallowed the `router.push` (user-reported "does not redirect after delete"); reworked so the parent owns what happens next. **Tests +9** → `npm run test` **187 pass** (was 178): `collection.test.ts` — `updateCollectionSchema` is the same object as `createCollectionSchema` + trims/requires name/normalises description (×2); `collections.test.ts` — `updateCollection` (no demo user → `null`/no write; happy path asserts exact `updateMany` + re-read `findFirst` shape; `count: 0` → `null` without re-reading) and `deleteCollection` (no demo user → `false`/no write; scoped `deleteMany`; foreign id → `false`) (×6); `post-json.test.ts` — passes the `method` arg through (×1). `npm run lint`, `npm run build` — both green (`/api/collections/[id]` shows as a new `ƒ` route). Browser-verified (Playwright, demo session, live Neon): detail-page Edit → modal pre-filled → PATCH 200 → header + toast update; detail-page Delete → confirm → toast + **redirect to `/collections`**, list down one, **all 18 items intact** (3 `CollectionItem` join rows cascaded); card three-dots menu (dashboard + `/collections`) → Edit refreshes the card in place, Delete stays on the page with the list refreshed, Favorite is inert (no console error); card body click still navigates to the detail page. Dev DB re-seeded afterward. Deferred (raised earlier, still open): shared `BackLink` / `pluralize()` / `ListPageShell` extraction across `/items/[type]`, `/collections`, `/collections/[id]`; collection favorite toggle; adding items to a collection from these pages; pagination.
- Global Search / Command Palette — Per `context/features/global-search-spec.md`. Cmd/Ctrl+K palette with all matching done client-side. **Data**: `getAllItems()` added to `src/lib/db/items.ts` (every demo-user item, `getRecentItems` without the `take`); `getSearchCollections()` + `SearchCollection` (`{ id, name, itemCount }` via `_count`) added to `src/lib/db/collections.ts`; new `src/lib/db/search.ts` `getSearchIndex()` = `Promise.all([getAllItems(), getSearchCollections()])` → `{ items, collections }`. Fetched in the already-`force-dynamic` `dashboard/layout.tsx` (so `router.refresh()` after any create/edit/delete re-runs it — no separate API route, no staleness) and threaded `DashboardShell` → `TopBar` as `searchItems` / `searchCollections`. **UI primitive**: new dep `cmdk@^1.1.1`; `src/components/ui/command.tsx` wraps cmdk (`Command` / `CommandDialog` / `CommandInput` / `CommandList` / `CommandEmpty` / `CommandGroup` / `CommandItem` / `CommandSeparator`) in the codebase's radix-ui/`cn`/token style, `CommandDialog` built on the shared `Dialog` primitive; `src/components/ui/dialog.tsx` `DialogContent` gained an opt-out `showCloseButton` prop (default `true`, existing callers unaffected) so the palette has no X. **Drawer reuse**: extracted `ItemBrowser`'s drawer state + `/api/items/[id]` detail-fetch/cache + `handleSaved` / `handleDeleted` into a shared hook `src/components/items/use-item-drawer.ts` (`useItemDrawer()`); `ItemBrowser` now just consumes it (mechanical, no behaviour change). **Palette**: `src/components/dashboard/CommandPalette.tsx` — `Items` + `Collections` groups; item rows show the type icon (`TYPE_ICON` + `palette().text`) + a whitespace-collapsed 100-char content/url/description preview; collection rows show `N items`; picking an item calls the shared `useItemDrawer().select` (opens the same `ItemDrawer`, full detail incl. favorite/pin/tags/collections), picking a collection `router.push`es `/collections/[id]`. `TopBar` search field became a `<button>` (Search icon + "Search items..." + `⌘K` kbd) that opens the palette; `TopBar` owns `paletteOpen` + a global `keydown` listener (`(metaKey||ctrlKey) && key==='k'`, `preventDefault`, toggle). **Search quality**: cmdk's built-in scorer is a subsequence match, so short queries like "test" matched almost every row (letters scattered through titles / the content previews loaded into `keywords`). Replaced via `<Command filter>` (new passthrough on `CommandDialog`) with `src/lib/command-filter.ts` `commandFilter(value, search, keywords?)` — every whitespace token in the query must be a literal case-insensitive **substring** of one of the row's fields or the row is dropped (score 0); ranking favours the first field (title) and earlier positions; score is the per-token mean. `CommandItem` `keywords` are `[title, typeName, ...tags, preview]` for items and `[name]` for collections; `value` stays `"<title/name> <id>"` for cmdk selection uniqueness. Follow-up (this session): `commandFilter` reworked to a **binary** match (score `1`/`0`, no field/position ranking) so cmdk's score-descending sort no longer reorders the result list on each keystroke — rows now stay in render order (items newest-first, then collections); the inert `useMemo` (`CommandPalette` previews) and `useCallback`s (`use-item-drawer`) were removed since the React Compiler is not enabled. **Tests +14** → `npm run test` **200 pass** (was 187), then **199** after the binary-filter rework swapped two ranking tests for one. `npm run lint`, `npm run build` — both green (`cmdk` bundled into the dashboard route). Browser-verified (Playwright, demo session, live Neon): ⌘K and clicking the search field both open it; grouped Items/Collections with icons + counts; ↑↓/Enter selection; item → drawer with full detail; collection → navigation to `/collections/[id]`; "No results found." empty state; stable ordering confirmed (`"e"` → 17 items in newest-first order + 4 collections; `"docs"` → 3 rows in insertion order). Note: the palette (and the TopBar) only exist on `/dashboard`. Deferred: debounced re-fetch / pagination for large libraries (whole dataset is shipped to the client); palette availability outside `/dashboard`.
- Command Palette — Stable Result Ordering (follow-up fix) — `src/lib/command-filter.ts` `commandFilter` reworked from a graded field/position score to a **binary** `1`/`0` match. cmdk sorts visible rows by descending filter score, so the graded score made the result list reorder on every keystroke (title hits jumping above body hits, earlier matches above later ones) — flicker. With one score for every match, `Array.prototype.sort`'s stability leaves rows in render order: items newest-first (DB `updatedAt desc`), then collections. Same match semantics as before (per-token, case-insensitive, literal substring, all tokens required); the fields are just concatenated before the substring test. Also removed the now-inert `useMemo` (`CommandPalette` `previews` map) and the three `useCallback`s (`use-item-drawer` `select` / `handleSaved` / `handleDeleted`) — the React Compiler isn't enabled, and none of them gated a re-render (`ItemDrawer` isn't `React.memo`, cmdk re-renders every row regardless). `command-filter.test.ts` swapped its two ranking tests for one asserting equal scores → `npm run test` **199 pass** (was 200). `npm run lint`, `npm run build` green. Browser-verified (Playwright, live Neon): `"e"` → 17 items newest-first + 4 collections, `"docs"` → 3 rows in insertion order, no console errors.
- Pagination — Per `context/features/pagination-spec.md`. Numbered pagination (page links + prev/next) on `/items/[type]`, `/collections/[id]` and the `/collections` list; each page fetches only its slice (`skip`/`take` + a `count`), never the whole dataset. **New `src/lib/pagination.ts`**: constants `ITEMS_PER_PAGE = 21`, `COLLECTIONS_PER_PAGE = 21`, `DASHBOARD_COLLECTIONS_LIMIT = 6`, `DASHBOARD_RECENT_ITEMS_LIMIT = 10`; `Paginated<T>` = `{ items, page, pageCount, total }`; `parsePageParam(raw)` (→ 1-based int, junk/`0`/`-3`/`2.5` → 1); `paginate(total, page, size)` → clamped `{ page, pageCount, skip, take }` (out-of-range clamps to the last page, never an empty page); `pageWindow(page, pageCount)` → page numbers with an `ELLIPSIS` marker where the run skips (all pages if ≤ 7). Full `pagination.test.ts`. **New `src/components/ui/pagination.tsx`** — server component, built on `Button` (`asChild` + `next/link`) + `ChevronLeft/Right`; current page is a filled `Button` with `aria-current`, others outline links, prev/next render as **disabled `<button>`s** (not links) at the ends, `page 1` links use the bare `basePath` (no `?page=1`); returns `null` for `pageCount <= 1`. **Data layer** (`src/lib/db/items.ts`): new private `getItemsPage(where, page)` runs `prisma.item.count` + a `findMany({ skip, take })`; `getItemsByType(typeId, page)` and `getItemsByCollection(collectionId, page)` now return `Paginated<ItemWithType>` (was `ItemWithType[]`). (`src/lib/db/collections.ts`): `fetchCollectionsWithStats` gained a `{ skip?, take? }` range arg (skip 0 omitted so it's absent on page 1); new `getCollectionsPage(page)` → `Paginated<CollectionWithStats>` (count + paged fetch); `getRecentItems` / `getRecentCollections` defaults now reference the `DASHBOARD_*` constants, and `dashboard/page.tsx` + `CollectionsSection` pass them instead of literal `10` / `6`. `getSidebarCollections` still returns all (the sidebar needs the full list). `getCollectionCount` left untouched (pre-existing, unused). **Pages** — all three take `searchParams: Promise<{ page?: string | string[] }>`, resolve `params` + `searchParams` together, feed `parsePageParam` into the query, switch the count line + empty-state check from `items.length` to `total`, and drop `<Pagination page pageCount basePath>` after `<ItemBrowser>` / the card grid (`basePath` = `/items/${type}`, `/collections/${id}`, `/collections`). `/collections/page.tsx` swapped `getSidebarCollections()` for `getCollectionsPage()`. **Tests +16** → `npm run test` **215 pass** (`items.test.ts` rewritten for the `Paginated` shape + `item.count` mock + skip/take + clamp cases; `collections.test.ts` +`getCollectionsPage` block + `collection.count` mock; new `pagination.test.ts`). `npm run lint`, `npm run build` green (routes unchanged, all already `ƒ`). Browser-verified (Playwright, demo session, live Neon) with temporary rows then cleaned up (DB back to 5 collections / 18 items): `/items/link` (31 items → 21 + 10 across 2 pages, prev disabled on p1 / next disabled on p2, `?page=99` clamps to p2), `/collections/[id]` (28 items → 21 + 7), `/collections` (25 collections → 21 + 4); `/items/snippet` (4 items) shows no pager. Note: the pager only renders once a list exceeds 21, so with the current seed data none of the three pages show it. Deferred: shared `ListPageShell` / `BackLink` / `pluralize()` extraction across the three list pages (raised earlier, still open); the command palette still ships its whole dataset to the client (separate from these server-paginated pages).
- Settings Page + Sidebar Link — New protected `/settings` route (`src/app/settings/page.tsx`, `force-dynamic`, `title` "Settings · DevStash"), added to the `src/proxy.ts` middleware matcher (`/settings` + `/settings/:path*`, alongside the existing `/profile` entries). The sidebar user dropdown (`SidebarUser`) gained a **Settings** item (lucide `Settings` icon) between Profile and Sign out — hidden when the rail is collapsed like the rest of that menu. The account actions moved off `/profile` onto `/settings`: `ChangePasswordForm` + `DeleteAccountDialog` `git mv`d from `src/components/profile/` to `src/components/settings/` (only the profile page imported them; `ProfileStats` stays in `profile/`; `ChangePasswordForm`'s doc comment updated profile→settings). The profile page keeps the avatar/badges/`ProfileStats` and its Account `<section>` is now a single card that links through to `/settings`. **New `requireProfileUser(callbackPath)`** in `src/lib/db/profile.ts` folds the `auth()` → session-id → `getProfileUser` → `redirect("/sign-in?callbackUrl=…")` dance that `/profile` and `/settings` were both duplicating into one helper — covers both failure cases (no session, session whose user row is gone), returns a `ProfileUser`, and `encodeURIComponent`s the callback path (the old inline code didn't). `profile.ts` now imports `redirect` (next/navigation) + `auth` (@/auth); its header comment broadened to "`/profile` and `/settings` pages". The profile page now awaits `requireProfileUser` then `getProfileStats(user.id)` sequentially instead of a `Promise.all` — one extra Neon round-trip on a `force-dynamic` page, judged worth the dedup. **New `src/lib/db/profile.test.ts`** (+3): no session → redirects with an encoded `callbackUrl` and never hits Prisma; session but missing user row → redirects; happy path → returns the user with `hasPassword` derived and no `password` field, no redirect (mocks `@/auth`, `@/lib/prisma`, and `next/navigation`'s throwing `redirect`). Ran `npm run test` **218 pass** (was 215), `npm run lint`, `npm run build` — all green (`/settings` builds as a `ƒ` route). Browser-verified (Playwright, demo session, live Neon): dropdown → Settings navigates to `/settings`; both the change-password card (form expands) and delete-account card render; profile page renders with the "Go to settings" link and no inline account actions; unauthenticated `GET /settings` and `/profile` both 302 → `/sign-in?callbackUrl=…` via the middleware. Deferred: the API routes `POST /api/auth/change-password` + `/delete-account` still do their own `auth()` guard (they return JSON 401, not a redirect, so `requireProfileUser` doesn't fit); a settings link on the profile page's Account card is the only cross-link (no reverse link from settings back to profile beyond "Back to dashboard").
- Editor Preferences Settings — Per `context/features/editor-settings-spec.md`. An "Editor preferences" card on `/settings` with five auto-saving controls (no save button): font size (`select`, 12/13/14/16/18px), tab size (`select`, 2/4/8), theme (`select`, `vs-dark` / `monokai` / `github-dark`), and word-wrap + minimap `role="switch"` toggles. Defaults: fontSize 13, tabSize 2, wordWrap **on**, minimap **off**, theme `vs-dark` (the last three per spec). **Schema**: `User.editorPreferences Json?` (`JSONB`, nullable — `null` means "use defaults"; seed doesn't touch it), migration `20260907171910_add_user_editor_preferences` via `prisma migrate dev` on the Neon dev branch. **`src/lib/editor-preferences.ts`** (pure, tested): `EditorPreferences` type, `DEFAULT_EDITOR_PREFERENCES`, `FONT_SIZE_OPTIONS` / `TAB_SIZE_OPTIONS` / `EDITOR_THEMES` / `EDITOR_THEME_OPTIONS`; `normalizeEditorPreferences(raw: unknown)` — the single gate turning the untyped JSON column (or a partial patch) into a complete valid object, dropping any field not in its option list; `monacoThemeName(theme)` → `devstash-<theme>`; `toMonacoEditorOptions(prefs)` → `{ fontSize, lineHeight: round(fontSize*1.55), tabSize, wordWrap: "on"|"off", minimap: { enabled } }`. **Validation** (`src/lib/validations/editor-preferences.ts`): `editorPreferencesSchema` — all fields required (the form sends the full object every change), numerics `.refine`d against the option lists, `theme` a `z.enum(EDITOR_THEMES)`; `UpdateEditorPreferencesInput`. **Data layer** (`src/lib/db/editor-preferences.ts`): `getEditorPreferences(userId: string | null | undefined)` — returns `DEFAULT_EDITOR_PREFERENCES` (no query) when no id, else `findUnique` → `normalizeEditorPreferences`; `updateEditorPreferences(userId, data)` — `prisma.user.update` (`data` cast `as unknown as Prisma.InputJsonObject` — a typed interface doesn't satisfy `InputJsonValue`'s index-signature) then re-normalise. **Session-scoped, not demo-user-scoped** — unlike the dashboard data layer, these key off the real signed-in user id (like `src/lib/db/profile.ts`). **Server action** (`src/actions/editor-preferences.ts`, `"use server"`): `updateEditorPreferences(input: unknown)` → `auth()` guard → `editorPreferencesSchema.safeParse` (→ `fieldErrors`) → query (imported aliased as `updateEditorPreferencesQuery`) → `{ success, data }` / `{ success, error }`; try/catch → generic error. **Context** (`src/components/editor-preferences/EditorPreferencesProvider.tsx`, `"use client"`): holds `preferences` seeded from an `initial` prop; `update(patch)` optimistically sets, calls the action, then `toast.success("Editor preferences saved")` or rolls back + `toast.error`. On success it does **not** re-apply `result.data` (would clobber a newer optimistic change mid-flight). A `latest` **ref** mirrors the newest optimistic state so back-to-back `update()` calls build `next` from it, not a stale render closure — this was a real bug in the first cut (rapid theme/font/minimap changes clobbered each other; DB ended up with only the last field's change on a stale base). Two hooks: `useEditorPreferences()` (strict — throws outside a provider; for the settings form) and `useEditorPreferencesValue()` (lenient — returns `DEFAULT_EDITOR_PREFERENCES` outside a provider; for `CodeEditor`). **UI** (`src/components/settings/EditorPreferencesForm.tsx`): local `selectClass` + a `Row` layout helper + a `Toggle` (`role="switch"` button, `aria-checked`); every control's `onChange` calls `update({ field })`; inputs `disabled={saving}`. **Monaco** (`src/components/items/monaco-themes.ts`): `registerMonacoThemes(monaco)` defines `devstash-vs-dark` (the app-tuned theme that previously lived inline in `CodeEditor` as `defineTheme`), plus compact hand-rolled `devstash-monokai` / `devstash-github-dark` palettes (Monaco only ships `vs` / `vs-dark` / `hc-*`). **`CodeEditor.tsx`**: dropped the module-level `LINE_HEIGHT` const + inline `defineTheme`; `estimateHeight(value, lineHeight)` now takes the derived line height; reads `useEditorPreferencesValue()`, spreads `toMonacoEditorOptions(preferences)` over the static `options` (so `fontSize` / `tabSize` / `wordWrap` / `minimap` / `lineHeight` are preference-driven — previously hard-coded 13 / 2 / "off" / disabled / 20), and sets `theme={monacoThemeName(preferences.theme)}` + `beforeMount={registerMonacoThemes}`. **Provider mounting**: `src/app/settings/page.tsx` fetches `getEditorPreferences(user.id)` and wraps the new card's form in `<EditorPreferencesProvider initial={…}>`; `src/app/dashboard/layout.tsx` awaits `auth()` first, then `Promise.all([… , getEditorPreferences(session?.user?.id)])` and wraps `<DashboardShell>` in the provider — so the Monaco editors in the item drawer / create+edit forms pick up the preferences. Cross-route live sync isn't a goal (the settings page and the dashboard each have their own provider instance); a change on `/settings` reaches an open dashboard editor after the next `router.refresh()` / navigation. **Tests +20** → `npm run test` **238 pass** (was 218): `editor-preferences.test.ts` (normalize: null/non-object → defaults, valid-value merge, drops out-of-range/wrong-type/unknown-theme, all three themes accepted; `toMonacoEditorOptions` mapping incl. wordWrap on/off; `monacoThemeName`); `validations/editor-preferences.test.ts` (well-formed passthrough, rejects off-list font/tab size, unknown theme, non-boolean toggle, missing fields); `db/editor-preferences.test.ts` (no id → defaults + no query; normalises the stored blob; null column → defaults; `update` writes the payload + returns the normalised round-trip — Prisma singleton mocked); `actions/editor-preferences.test.ts` (unauth → error + no DB; invalid payload → `fieldErrors`; happy path calls the query with `session.user.id` + validated data; query throws → generic error — `@/auth` + query mocked). `npm run lint`, `npm run build` — both green (`/settings` still `ƒ`; `prisma generate` picks up the new column). Browser-verified (Playwright, demo session, live Neon): settings card renders with defaults; three rapid changes (font→18, tab→2, minimap off) each toast "saved" and all five values persist correctly to `User.editorPreferences` (the ref fix — before it, they raced); reload re-hydrates the form from the DB; opening a snippet in the dashboard item drawer shows Monaco with the Monokai palette + larger font; no console errors. Reset the demo user's `editorPreferences` to `NULL` afterward. Deferred: cross-route live preference sync without a refresh; per-item / per-language editor overrides; exposing font family or line-number visibility; a preview editor inside the settings card.
- Favorites Page — Per `context/features/favorites-spec.md`. New protected `/favorites` route showing the demo user's favorited items and collections in a compact, terminal-style list (not cards). **Data layer**: `getFavoriteItems()` in `src/lib/db/items.ts` — demo-user items `where: { isFavorite: true }`, `orderBy: { updatedAt: "desc" }`, `ITEM_INCLUDE`, mapped to the existing `ItemWithType` (same shape as `getRecentItems`, just filtered + uncapped). `FavoriteCollection` interface + `getFavoriteCollections()` in `src/lib/db/collections.ts` — favorited collections newest-first, `select` `{ id, name, updatedAt, _count: { items } }` → `{ id, name, itemCount, updatedAt }` (the `_count` aggregate mirrors `getSearchCollections`). Both return `[]` without querying when there's no demo user. **Route**: `src/app/favorites/page.tsx`, `export const dynamic = "force-dynamic"`, `metadata.title` "Favorites · DevStash"; `Promise.all([getFavoriteItems(), getFavoriteCollections()])`, "Back to dashboard" link + amber-filled `Star` heading + `N items, M collections` count line, delegates the list to `<FavoritesList>`. **Component**: `src/components/favorites/FavoritesList.tsx` (`"use client"`). Consumes the shared `useItemDrawer()` hook (state + `/api/items/[id]` fetch/cache + `handleSaved`/`handleDeleted`) and renders `<ItemDrawer>` — same full-detail drawer as the dashboard / command palette. Two `font-mono` `<section>`s ("Items", "Collections"), each with an uppercase heading + count; an empty section is omitted, and the both-empty case renders a single muted "No favorites yet." line. A local `Row` helper renders each entry as a full-width `<button>`: `icon · truncated title · uppercase type badge · right-aligned formatShortDate`, thin `border-b border-border` dividers, `hover:bg-muted/50` + `focus-visible:bg-muted/50`, no card chrome. Item icons come from `TYPE_ICON` + `palette(type.color).text` (`type-presentation.ts`); item click → `drawer.select(item)`; collection icon is a grey lucide `Folder`, badge is the literal "collection", click → `router.push('/collections/${id}')`. **Nav**: `src/components/dashboard/TopBar.tsx` gained a ghost `size="icon"` `Star` button (`asChild` + `next/link` to `/favorites`, `aria-label="Favorites"`) as the first element of the right-hand action group, before "New Collection". The `/favorites` page is standalone (its own "Back to dashboard" link, like `/collections` and `/settings`) — the TopBar only exists on `/dashboard`, so the star button is the entry point. **Protection**: `/favorites` added to the `src/proxy.ts` middleware matcher (bare path, no `:path*` — no sub-routes). **Tests +4** → `npm run test` **242 pass** (was 238): `items.test.ts` — `getFavoriteItems` (no demo user → `[]` + no query; queries `{ userId, isFavorite: true }` newest-first with the include, maps tags flat); `collections.test.ts` — `getFavoriteCollections` (no demo user → `[]` + no query; exact `findMany` `select` shape + maps `_count.items` → `itemCount` and passes `updatedAt` through). `npm run lint`, `npm run build` — both green (`/favorites` builds as a `ƒ` route). Browser-verified (Playwright, demo session, live Neon; demo data had 6 favorite items + 2 favorite collections, set earlier via Neon MCP — ~⅓ of 18 items / 5 collections): page renders both sections with correct counts in the dense mono style; clicking `groupBy utility` opens the `ItemDrawer` with full detail (favorite/pin/copy/edit/delete, description, Monaco body, collections, dates); clicking `Terminal Commands` navigates to `/collections/[id]`; the TopBar star navigates to `/favorites`; unauthenticated `curl /favorites` → 302 `/sign-in?callbackUrl=…` via the middleware. Deferred: no favorite-toggle UI for collections yet (still deferred from Collection Edit/Delete) — this page only reads `isFavorite`; the empty state can't be reached through the UI until items can be un-favorited from here or the list is empty; no pagination (favorites are assumed to be a small set).
- Favorite Toggle — Drawer, Collection Page & Cards — Per `context/current-feature.md`. Wired up the favorite control in the three places it was an inert placeholder since "Collection Edit / Delete": the item drawer's `Star` `ActionButton`, the `/collections/[id]` header, and the `CollectionCard` three-dots menu (dashboard + `/collections`). Each persists `isFavorite` then `router.refresh()`es so the sidebar Favorites group, `/favorites` and the dashboard favorite stat cards all follow — including dropping a row from a list that only shows favorites. **Items** (Server Actions, like `updateItem`/`deleteItem`): `setItemFavorite(id, isFavorite)` in `src/lib/db/items.ts` — demo-user-scoped `updateMany({ where: { id, userId }, data: { isFavorite } })`, `count === 0` → `null`, else re-reads via `getItemDetail(id)` so the drawer reconciles without a second fetch (mirrors `updateItem`'s "return fresh detail" shape). `setItemFavorite` action in `src/actions/items.ts` — `auth()` + non-empty-id + `typeof isFavorite === "boolean"` guards → query → `{ success, data }` / `{ success, error }`; no Zod schema (id + boolean). **Collections** (kept as API routes — the deliberate divergence): `setCollectionFavorite(id, isFavorite)` in `src/lib/db/collections.ts` — scoped `updateMany` → re-read `CollectionSummary`, `count === 0` → `null` (mirrors `updateCollection`). New `favoriteCollectionSchema = z.object({ isFavorite: z.boolean() })` in `src/lib/validations/collection.ts` (a separate schema — the shared `updateCollectionSchema` is aliased to the create schema and requires `name`). New route `src/app/api/collections/[id]/favorite/route.ts` (`PATCH` only) — same `auth()` → `readJsonBody`/`INVALID_JSON` → `favoriteCollectionSchema.safeParse` → `validationErrorResponse` → `setCollectionFavorite`, `null` → 404, else `{ success, data }` — same helpers + envelope as the sibling `PATCH /api/collections/[id]`. **UI**: `ItemDrawer.tsx` — the `Star` `ActionButton` now has `onClick` → `handleToggleFavorite` (reads `detail.isFavorite` for the next value, so it's `disabled={!detail || favoritePending}` like the Edit button — sidesteps a race with the in-flight `select` detail fetch); a `favoriteOverride` state gives an instant optimistic flip, cleared once `onSaved` (→ `useItemDrawer.handleSaved`, which already folds `isFavorite` into `summary` + `router.refresh()`) lands the fresh detail; both new bits of state join the existing render-phase reset keyed on the open item. Displayed value is `favoriteOverride ?? detail?.isFavorite ?? summary?.isFavorite ?? false`. New shared hook `src/components/collections/use-collection-favorite.ts` — `useCollectionFavorite(collection)` returns `{ pending, isFavorite, toggle }`; `toggle` does `postJson(\`/api/collections/${id}/favorite\`, { isFavorite: next }, "PATCH")` → `toast.success("Added to favorites" / "Removed from favorites")` → `router.refresh()` (no local optimistic — the server-rendered `collection` prop updates on the refresh). `CollectionActionsMenu` — the "Favorite" `DropdownMenuItem` gained `onSelect={() => void favorite.toggle()}`, label flips to "Unfavorite" + amber-filled star when favorited. `CollectionDetailActions` — the header star `<button>` gained `onClick`, `aria-pressed`, `disabled={favorite.pending}`, an "Unfavorite collection" aria-label when set, and `text-amber-400` + `fill-amber-400` when favorited. `ItemCard` was **not** touched — it has no action affordance; items toggle via the drawer (confirmed as the reading of "the cards" = the collection card menu). Drawer Pin button stays inert (out of scope). Note: the favorite write bumps `updatedAt` (Prisma `@updatedAt` on `updateMany`), so favoriting re-sorts a row to the top of recency-ordered lists — same as every other edit. **Tests +14** → `npm run test` **256 pass** (was 242): `items.test.ts` — `setItemFavorite` (no demo user → `null`/no write; scoped `updateMany` + returns the `getItemDetail` round-trip; `count: 0` → `null` without re-reading) — mock gained `item.findFirst`/`item.updateMany`; `actions/items.test.ts` — `setItemFavorite` action (unauth, missing id, non-boolean → error + no query; happy path passes `(id, bool)` + returns detail; `null` → not-found; throw → generic error); `collections.test.ts` — `setCollectionFavorite` (no demo user → `null`/no write; scoped `updateMany` + re-read `findFirst` shape; `count: 0` → `null` without re-reading); `validations/collection.test.ts` — `favoriteCollectionSchema` (accepts booleans; rejects missing / string / number). `npm run lint`, `npm run build` — both green (`/api/collections/[id]/favorite` builds as a new `ƒ` route). Browser-verified (Playwright, demo session, live Neon; baseline 6 favorite items / 2 favorite collections): dashboard card menu → favorite "Design Resources" → favorite-collections stat card 2→3, row moved into the sidebar Favorites group; `/collections/[id]` header → unfavorite → button label + `aria-pressed` flip, star removed, "Removed from favorites" toast; item drawer on `/favorites` → toggle "groupBy utility" off → row drops from the list (6→5), toggle back on → reappears (6). No new console errors (only the pre-existing pg `sslmode` warning). DB state restored to the original 6 / 2. Deferred: no favorite affordance on `ItemCard` (items toggle via the drawer); drawer Pin toggle still inert; the collection spots refresh rather than optimistically flip (server-rendered prop, brief delay); adding items to a collection from these pages; the long-standing `ListPageShell` / `BackLink` / `pluralize()` extraction across the three list pages.
- Favorites Page — Client-Side Sorting — Per `context/current-feature.md`. A sort control on `/favorites` that reorders the list entirely in the browser — no query, no refetch; the page still ships the DB-ordered arrays. **New `src/lib/favorites-sort.ts`** (pure, framework-free, unit-tested): `type FavoriteSortKey = "name" | "date" | "type"`, `type SortDir = "asc" | "desc"`, `interface FavoriteSort { key; dir }`; `FAVORITE_SORT_OPTIONS` (`Date` / `Name` / `Type`, in that display order); `defaultDirForKey(key)` → `"desc"` for `date`, `"asc"` for text; `DEFAULT_FAVORITE_SORT = { key: "date", dir: "desc" }` (today's most-recently-favorited-first order). `sortFavoriteItems(items, key, dir)` / `sortFavoriteCollections(collections, key, dir)` each return a **new** array (`[...list].sort(...)`, never mutate); comparators are written **ascending** and `dir === "desc"` negates the result; `name` via `localeCompare(a, b, undefined, { sensitivity: "base" })` on `title`/`name`, `type` via `localeCompare` on `type.name` tie-broken by `title`, `date` via `updatedAt.getTime()` difference. `sortFavoriteCollections` has no type field, so `key: "type"` falls back to `name`. Type-only imports of `ItemWithType` / `FavoriteCollection` from `@/lib/db/*` — no server surface pulled in. **`FavoritesList.tsx`**: `const [sort, setSort] = useState(DEFAULT_FAVORITE_SORT)` (in-memory only — no localStorage / URL param). A right-aligned control row above the two sections in the page's dense `font-mono text-xs text-muted-foreground` style: a `<label htmlFor>` + `<select id="favorites-sort">` (options from `FAVORITE_SORT_OPTIONS`; `onChange` → `setSort({ key, dir: defaultDirForKey(key) })` so switching key resets to that key's natural direction) + a `size-7` bordered icon `<button>` toggling `dir`, showing lucide `ArrowUp` / `ArrowDown`, with an `aria-label` that states the current direction and what the click does ("Sorted ascending — switch to descending" / vice-versa). Rendered only inside the existing non-empty branch (empty state untouched). `const sortedItems = sortFavoriteItems(items, sort.key, sort.dir)` / `sortedCollections = …` computed inline after the empty-state early return and mapped by the two `<Section>`s (small lists — no `useMemo`, consistent with the codebase since the React Compiler is off); the section headings/counts now read off the sorted arrays. `Section` / `Row` helpers, the `useItemDrawer` wiring and `favorites/page.tsx` are unchanged. **Tests +11** → `npm run test` **267 pass** (was 256): new `favorites-sort.test.ts` — `defaultDirForKey` + `DEFAULT_FAVORITE_SORT`; `sortFavoriteItems` name (both dirs, case-insensitive), date (asc = oldest first / desc = newest first), type (grouped by type name, tie-broken by title), no-mutation, stability for equal keys; `sortFavoriteCollections` name, `"type"`→`"name"` fallback, date, no-mutation. `npm run lint`, `npm run build` — both green (`/favorites` still `ƒ`; nothing else moved). Browser-verified (Playwright, demo session, live Neon; 6 favorite items / 2 collections): control renders in the mono style; Date-desc reproduces the prior order; Name → A–Z across both sections (collections React Patterns → Terminal Commands); switching key flips the direction button to that key's default; Type → items grouped command / link / prompt / snippet with title tie-break, collections fall back to name; the direction toggle reverses both sections and updates its label. Only the pre-existing pg `sslmode` console warning. Deferred: persistence (localStorage / `?sort=` URL param); a per-section independent sort; the same control on the dashboard / `/collections` lists; secondary sort keys.
- Homepage Mockup — Per `context/features/homepage-mockup-spec.md`. Standalone static marketing prototype under `prototypes/homepage/` (`index.html`, `styles.css`, `script.js`) — **not wired into the Next.js app**, lives outside `src/` so `npm run build`/`lint`/`test` never touch it and no unit tests apply. Dark theme; item-type accent CSS vars (snippet `#3b82f6` / prompt `#f59e0b` / command `#06b6d4` / note `#22c55e` / file `#64748b` / image `#ec4899` / url `#6366f1`). **Sections**: fixed nav (logo, Features/Pricing links, Sign In / Get Started; `.is-scrolled` opacity+border toggled by a passive scroll listener); hero text ("Stop Losing Your **Developer Knowledge**", gradient `<span>`, status pill, sub, two CTAs); **hero "chaos → order" visual** (`.flow` 3-col grid → 1-col ≤900px) — left `.chaos__field` with 8 inline-SVG icons (Notion/GitHub/Slack/VS Code/browser-tabs/terminal/text-file/bookmark) driven by one `requestAnimationFrame` loop (per-icon x/y/vx/vy, wall bounce, time-based rotate + scale pulse, gentle cursor repel with harder damping + lower speed cap while hovering; `prefers-reduced-motion` skips the loop and CSS neutralises animations), centre a CSS-`@keyframes` pulsed arrow (pulse on the inner `<svg>` so the mobile 90° container `rotate` isn't clobbered), right a populated dashboard preview (sidebar nav, mock `Search… ⌘K` field + `+ New` button, 9 cards each with a type-colored chip + 2 skeleton bars + colored top border); **Features** grid (6 cards `repeat(3,1fr)` → 2 → 1, each in its item-type color); **AI section** (`.ai__grid` 2-col → 1-col; left "PRO FEATURE" badge + green checklist, right a `debounce.ts` editor mockup with staggered "AI Generated Tags"); **"How it works"** two-column row (`.how__grid` 1.1fr/1fr → 1-col) — left an `<ol>` of 3 `counter()`-numbered steps (Capture / Organize / Recall, blue/green/indigo badges), right a mono `.how__visual` panel of 3 color-left-bordered stage rows; **Pricing** (`.plans` 2-col → 1-col; Free $0 vs Pro, "Most Popular" badge on the featured card, monthly/yearly `.toggle` that rewrites the Pro `$8/mo` ↔ `$72/yr` amount/cycle/period in JS); closing **CTA**; **footer** (brand + 3 link columns + `© <year>` injected by JS). **`script.js`** (one IIFE): footer year, navbar scroll opacity, scroll-reveal (`.reveal` → `.is-visible` via a plain rAF-throttled scroll check on `getBoundingClientRect().top < innerHeight*0.92` rather than `IntersectionObserver`, so anchor-jumped-past sections still reveal), pricing toggle, chaos physics. **Responsive**: chaos/arrow/preview stack vertically and the arrow rotates 90° to point down ≤900px; grids collapse to one column; mobile nav buttons `white-space: nowrap` + tighter padding <620px. `overflow-x: clip` on **both `html` and `body`** — the root clip fixed a reported left-shift where a stray overflowing child could still scroll `<html>` sideways. Browser-verified (Playwright, desktop 1280/1440 + mobile 390): every section renders; nav/hero centred with equal gutters and `scrollWidth === clientWidth` at all widths; pricing toggle swaps `$8/mo` ↔ `$72/yr`; cursor-repel measurably pushes icons away (avg distance 117 → 159 px) and is gentler after tuning; `prefers-reduced-motion` disables the loop; only console noise is a `favicon.ico` 404. Iterations this session after the first cut: chaos icons 46 → 56 px + softened/slowed hover repel; filled the previously-empty dashboard preview (card content + toolbar); added the "How it works" 2-col row; the centering/overflow fix; mobile nav-button wrap fix. Deferred: not integrated as a real Next.js route; icons are hand-rolled SVGs, not brand-accurate logos; no automated tests (static prototype, outside the Vitest globs).
- Homepage — Real App Route — Per `context/features/homepage-spec.md`. Turned the `prototypes/homepage/` mock-up into the real marketing homepage at `/`, rebuilt on the app stack (Next server/client components, Tailwind v4, shadcn `Button`/`Badge`, `lucide-react`, theme tokens); the prototype files are untouched (visual reference only). **`src/lib/home-content.ts`** — single data module, no JSX: `ITEM_TYPES` (7 × `{ name, hex, icon, iconBg, topBorder, swatch }`, every colour field a **literal arbitrary-value Tailwind class** kept as a string so the JIT emits it — `swatch` (solid `bg-[hex]`) is one field beyond the spec's list, for the preview-card squares) + `itemType()` lookup, `PREVIEW_CARD_TYPES`, `FEATURES` (6 × `{ title, blurb, typeName, icon: LucideIcon }`), `AI_CAPABILITIES` (4), `HOW_STEPS` (3), `PRICING` (`{ monthly, yearly }` mirroring `script.js` `PRICES`) + `FREE_FEATURES` / `PRO_FEATURES`, `FOOTER_COLUMNS`, `CHAOS_SOURCES` (8 labels). **`src/components/home/`** (13 files): `SectionHeading` (server), `FeatureCard` (server — `border border-border border-t-[3px]` + `type.topBorder` via `cn`, mirrors the repo's existing `border-l-2` + `border-l-<color>` accent pattern), `Reveal` (**client** — `IntersectionObserver` adds `opacity-100 translate-y-0` then disconnects; the reduced-motion branch does **not** `setState` in the effect body — it returns early and CSS `motion-reduce:opacity-100 motion-reduce:translate-y-0 motion-reduce:transition-none` pins it visible, which resolves the `react-hooks/set-state-in-effect` lint error the first cut hit), `HomeNav` (**client** — `scroll` listener toggles `bg-background/90 border-border` vs `bg-background/40 border-transparent`; logo `/`, `#features`/`#pricing` anchors hidden `< sm`, `Sign In` → `/sign-in` + `Get Started` → `/register`, or a single `Go to dashboard` → `/dashboard` when `signedIn`; all `<Button asChild>` + `next/link`), `ChaosOrderFlow` (**client** — `script.js` physics ported into a `requestAnimationFrame` `useEffect`: per-icon `x/y/vx/vy/phase/spin`, wall bounce, drift wander, cursor-repel with harder damping + lower speed cap while hovering; nodes in a `useRef` array, imperative `el.style.transform` writes — the only inline-style exception per the coding standard; `resize` listener re-measures the field; `prefers-reduced-motion` → lays the 8 icons out in a static 4×2 grid via one-shot transform writes and returns, no loop/listeners; cleanup cancels the frame + removes listeners. Also renders the lucide `ArrowRight` (`max-md:rotate-90` — Tailwind v4 emits the `rotate` CSS property, verified `rotate: 90deg` at 390px — plus `animate-pulse motion-reduce:animate-none`) and a static `DashboardPreview` sub-component: 92px sidebar nav + `Search… ⌘K` bar + `+ New` pill + 9-card grid, each card `border-t-2` + its `type.topBorder` and a `type.swatch` square; container `md:grid-cols-[1fr_auto_1fr]` → 1 col below `md`), `Hero` (server — status pill, `<h1>` with the gradient `<span>` (`bg-gradient-to-r from-[#3b82f6] via-[#6366f1] to-[#ec4899] bg-clip-text text-transparent`), sub-copy, two CTAs (primary → `/register`, or `/dashboard` + "Go to dashboard" when `signedIn`; secondary `#features`), renders `<ChaosOrderFlow />` inside a `<Reveal>`; own top padding `pt-[8.5rem]` for the fixed nav), `FeaturesSection` (server — `id="features" scroll-mt-24`, `SectionHeading` + `FEATURES.map` in `grid gap-5 sm:grid-cols-2 lg:grid-cols-3`, each card wrapped in `<Reveal className="h-full">`), `AiSection` (server — `id="ai" scroll-mt-24`, `lg:grid-cols-2`: left `Badge variant="outline"` "Pro Feature" + `Check`-bulleted `AI_CAPABILITIES` + `<Button asChild>` → `#pricing`; right `AiEditorMock` — a `font-mono` `debounce.ts` card with traffic-light dots, hand-coloured token `<span>`s inside a `<pre>` built from `\n`-bearing string literals, and an "AI Generated Tags" row of outline `Badge`s), `HowItWorksSection` (server — `lg:grid-cols-[1.1fr_1fr]`: numbered-badge `<ol>` of `HOW_STEPS` (blue/green/indigo) + a mono panel of 3 `border-l-[3px]` colour-keyed stage rows), `PricingSection` (server — `id="pricing" scroll-mt-24`) → `PricingPlans` (**client** — `useState<"monthly" | "yearly">`, segmented toggle of two `<button aria-pressed>`s, `PlanCard` sub-component; Free `$0` / `FREE_FEATURES` / outline "Get started" → `/register`, Pro highlighted (`border-[#6366f1]/55` + glow) with a "Most Popular" `Badge`, amount/cycle/period from `PRICING[period]`, `PRO_FEATURES`, primary "Go Pro" → `/register`), `CtaSection` (server — `border-y` gradient panel, heading + line + `<Button asChild size="lg">` "Create your stash" → `/register`), `HomeFooter` (server — brand + tagline, `FOOTER_COLUMNS` link columns (Product → `#features`/`#pricing`/`#ai`, Resources/Company → `#` placeholders), `© {new Date().getFullYear()} DevStash`). **`src/app/page.tsx`** — server component; `metadata` (title + description lifted verbatim from the prototype `<head>`); `await auth()` → `signedIn` threaded to `HomeNav` + `Hero`; a fixed `aria-hidden` ambient radial-gradient glow div (arbitrary Tailwind gradients); composes the 8 sections in spec order inside a `relative flex min-h-screen flex-col overflow-x-clip` wrapper — `auth()` makes `/` a `ƒ` (dynamic) route. **`src/app/globals.css`** — added `scroll-behavior: smooth` to the `html` base rule; sections carry `scroll-mt-24` for the fixed-nav anchor offset (no global `scroll-padding-top`). **No new Vitest suites** — static data only, no helpers, per the spec. `npm run lint` clean (the lone remaining warning is the **pre-existing** unused-`i` in `prototypes/homepage/script.js`), `npm run build` green (`/` shows as `ƒ`), `npm run test` **267 pass** (unchanged). Browser-verified (Playwright, dev server; the test browser carried a demo session so the signed-in nav path rendered): every section renders; `scrollWidth === clientWidth` at 1280 and 390; nav flips opaque after `scrollY > 12`; `<Reveal>` blocks fade in on scroll (features wrapper opacity → 1); pricing toggle swaps `$8 /mo` + "billed monthly" ↔ `$72 /yr` + "billed yearly ($6/mo)" and flips `aria-pressed`; all 8 chaos icons animate and measurably repel from the cursor (mean distance 116 → 139 px); under emulated `prefers-reduced-motion` the chaos icons snap to a static grid with no rAF churn, the reveal blocks are visible immediately, and the console is error-free; at 390 the chaos/arrow/preview stack to one column, the arrow rotates (`rotate: 90deg`), and the nav's Features/Pricing links are `display: none`. Deviations from spec: `Reveal`'s reduced-motion path is CSS-class-based rather than a synchronous effect `setState` (lint); `ITEM_TYPES` has an extra `swatch` field; the hero primary CTA also swaps to "Go to dashboard" when `signedIn` (spec listed this as optional). **Note:** `.env.example` had an unrelated pre-existing uncommitted change (Stripe env-var stubs) — deliberately **excluded** from this feature's commit; still uncommitted on `main`. Deferred: no shared `ListPageShell` / layout primitives with the rest of the app; chaos icons are generic lucide stand-ins, not brand logos; no component/DOM tests (codebase convention); the page is grayscale theme tokens + the ITEM_TYPES accents + the ambient glow (the app theme is grayscale, unlike the prototype's navy).
- Responsive Dashboard Top Bar — Per `context/features/responsive-topbar-spec.md`. The dashboard top bar (`src/components/dashboard/TopBar.tsx`) overflowed on small screens: at 375 px the `ml-auto` action group (`New Collection` + `New Item` + Favorites star) was ~280 px wide and ran ~100 px past the viewport, so the page scrolled sideways (`document.scrollWidth` 474 vs client 375), "New Item" was fully clipped off-screen, "New Collection" was clipped, and the search field was squished to ~123 px with its "Search items..." placeholder wrapping onto two lines. Only comfortable from ~768 px up. **Fix — `TopBar.tsx` only** (self-contained; no `DashboardShell`/layout/prop changes, no new deps — reused the existing `src/components/ui/dropdown-menu.tsx` primitive). **(1) Consolidated create menu below `sm`:** the two labelled buttons are `hidden sm:inline-flex`; a new `DropdownMenu` whose `size="lg"` `+ New` trigger is `sm:hidden` opens a two-item menu ("New item" / "New collection") whose `onSelect` handlers set `newItemOpen` / `newCollectionOpen` — same radix-dropdown-opens-radix-dialog pattern as `CollectionActionsMenu`, focus handoff works. **(2) Icon-only search below `sm`:** a `size="icon"` ghost `Search` button (`sm:hidden`, `aria-label="Search items and collections"`) opens the palette; the full field is now `hidden … sm:flex` and gained `min-w-0` + a `truncate` span + `whitespace-nowrap` so it can shrink without wrapping; the `⌘K` `<kbd>` is `hidden md:block` (no meta key on phones) and its right padding is `pr-3 md:pr-12` to match. **(3) Overflow guards:** header is `gap-2 px-3 sm:gap-3 sm:px-4` (was `gap-3 px-4`); `shrink-0` added to the sidebar-toggle buttons, the mobile search icon, and the `ml-auto` action group. Favorites star unchanged (already a compact icon button). No Vitest (client component — outside the `src/{actions,lib}` test scope); `npm run test` **267 pass** (unchanged), `npm run lint` clean (only the pre-existing `prototypes/homepage/script.js` unused-`i` warning), `npm run build` green. Browser-verified (Playwright, demo session, live dev server): **320 px & 375 px** — `document.scrollWidth === clientWidth` (overflow 0; action group now ends at x≈308 / x≈363 within the viewport), layout is `[≡] [🔍] … [★] [+ New]`, nothing clipped; the `+ New` menu opens both "New item" and "New collection" dialogs; the mobile search icon opens the command palette. **640 px** (the `sm` boundary) — full field + both labelled buttons, overflow 0. **1280 px** — full search field with `⌘K`, star, `New Collection` (outline) + `New Item` (primary), visually identical to before. No console errors. **Note:** the pre-existing unrelated uncommitted changes on `main` (`.env.example` Stripe stubs, untracked `.claude/agents/ui-reviewer.md`) were again deliberately excluded from this feature's commit. Deferred: the `sm`-and-up bar is still fairly dense between ~640–768 px (fits, but tight); no shared layout primitives extraction; drawer Pin toggle and other long-standing deferrals untouched.
- Unified Folder Logo + Top Nav on Auth Pages — Per `context/current-feature.md` (inline spec). Three presentational changes, no data layer / deps / schema / routes. **(1) `HomeNav.tsx`** — the logo mark `<span class="font-mono text-[#3b82f6]">&lt;/&gt;</span>` → `<Folder className="size-5 text-[#3b82f6]" />` (lucide, newly imported). The `Features` / `Pricing` links changed from `<a href="#features">` to `<Link href="/#features">` / `/#pricing` so they resolve from any route (on `/` a same-path hash `<Link>` just scrolls; off `/` it navigates home then scrolls) — this also cleared the `@next/next/no-html-link-for-pages` error the leading-slash `<a>` form triggers. **(2) `/sign-in` + `/register` pages** — now render `<HomeNav signedIn={false} />` (both are async server components that `redirect("/dashboard")` when a session exists, so signed-out is the only reachable state; `HomeNav` is `"use client"` but nests fine under a server page). Return wrapped in a fragment; `<main>` gained `pt-24` so the fixed `h-16` nav doesn't overlap the centered card. The card's logo went from a boxed icon (`<Link class="flex size-9 … rounded-lg bg-primary text-primary-foreground"><Layers class="size-5" /></Link>`) to a bare `<Folder className="size-5 text-[#3b82f6]" />` + `<span>DevStash</span>` inside a `flex items-center gap-2 font-extrabold tracking-tight` link — matching `HomeNav`'s treatment. `Layers` import dropped from both pages, `Folder` + `HomeNav` added. **(3) `Sidebar.tsx` brand row** (`h-14`, renders in both the desktop rail and the mobile `DashboardShell` drawer) — dropped the `<div class="flex size-7 … rounded-md bg-primary text-primary-foreground">` wrapper + `<Layers class="size-4" />`; now a bare `<Folder className="size-5 shrink-0 text-[#3b82f6]" />` (the `Folder` import already existed for collection rows; `Layers` import removed). The row gained `collapsed && "justify-center"` (via `cn`) so the lone icon centers in the `w-16` collapsed rail; the `!collapsed && <span>DevStash</span>` wordmark is unchanged. **Result** — one consistent boxless logo (blue `#3b82f6` folder + "DevStash" wordmark) across the home nav, both auth pages, and the dashboard sidebar. No new Vitest suites (client/server components, outside the `src/{actions,lib}` scope). `npm run test` **270 pass** (unchanged — count had already moved 267→270 from other work on `main`), `npm run lint` clean (only the pre-existing `prototypes/homepage/script.js` unused-`i` warning), `npm run build` green (`/sign-in`, `/register` still `ƒ` — they already called `auth()`). Browser-verified (Playwright, live dev server — the running server had gone into a stale-Turbopack state mid-edit throwing `ReferenceError: Layers is not defined` from a cached chunk; killed it, `rm -rf .next/dev`, restarted `npm run dev` → clean): `/sign-in` + `/register` show the full top nav (folder logo, Features/Pricing, Sign In/Get Started) with the card clear of the nav and a bare blue-folder + "DevStash" card logo; below `sm` the Features/Pricing links collapse (HomeNav's existing `hidden sm:flex`) with no horizontal overflow at 390; homepage nav folder logo renders and a "Features" click → `/#features` + scrolls (scrollY ≈ 898); dashboard sidebar brand is a bare blue folder + "DevStash" in both expanded and collapsed (icon centered) rail; `document.scrollWidth === clientWidth` at 1280 and 390; no console errors (only the pre-existing pg `sslmode` warning). Signed in as the demo user (`demo@devstash.io` / `12345678`) to check the collapsed rail. **Note:** the pre-existing unrelated changes on `main` — `.env.example` Stripe stubs (modified), untracked `.claude/agents/ui-reviewer.md`, `context/features/stripe-phase-{1,2}-spec.md`, `context/research/stripe-integration-research.md` — were again deliberately excluded from this feature's commit. Deferred: `/forgot-password` + `/reset-password` still use the old boxed `Layers` card logo and have no top nav (out of scope — user asked only for sign-in / sign-up); the redundant "Sign In" / "Get Started" nav buttons show even while on the sign-in / register page (accepted — the logo link home is the point).
- Stripe Phase 1 — Core Infrastructure — Per `context/features/stripe-phase-1-spec.md`. Foundation for **DevStash Pro** subscriptions ($8/mo, $72/yr); **no** webhooks / checkout / UI / gating (that's Phase 2, `context/features/stripe-phase-2-spec.md`). **Dependency**: `npm install stripe` → `stripe@^22.6.1` (Node SDK v22, ≥ the spec's v19 floor); no `@stripe/stripe-js` (hosted Checkout in Phase 2 needs no publishable key). **Schema + migration**: `prisma/schema.prisma` `User` — `stripeCustomerId String?` gained `@unique`, added `stripePriceId String?` + `stripeCurrentPeriodEnd DateTime?` (left `isPro` / `stripeSubscriptionId`). `prisma migrate dev` **refused in this non-interactive shell** (the `@unique`-on-existing-column warning forces a confirmation prompt, and Prisma 7 aborts `migrate dev`/`--create-only` entirely when it can't prompt), so the migration was produced out-of-band: `npx prisma migrate diff --from-config-datasource --to-schema prisma/schema.prisma --script` (Prisma 7's renamed flags — `--from-schema-datasource` was removed) → wrote `prisma/migrations/20260907214553_add_stripe_subscription_fields/migration.sql` (2 `ADD COLUMN` + `CREATE UNIQUE INDEX "User_stripeCustomerId_key"`) → `npx prisma migrate deploy` applied it to the Neon **development** branch → `npx prisma migrate status` "up to date" (5 migrations). `npx prisma format` re-aligned the `User` block. **New modules** (all under `src/lib/stripe/`, pure/server-only, no `NEXT_PUBLIC_`): `client.ts` — `getStripe(): Stripe | null` lazy singleton copying `getRedis()`'s exact memoisation (`let stripe: Stripe | null | undefined`; `undefined` = unresolved, `null` = no `STRIPE_SECRET_KEY`; `console.warn` **once**; **never throws** so Phase 2 endpoints can 503), constructs with `apiVersion: Stripe.API_VERSION` — **the spec's `Stripe.LATEST_API_VERSION` does not exist in SDK v22** (it was renamed; `Stripe.API_VERSION` is the typed literal `"2026-08-26.dahlia"` and satisfies the `LatestApiVersion` config type); `isStripeConfigured()` = `Boolean(STRIPE_SECRET_KEY && STRIPE_WEBHOOK_SECRET)`. `plans.ts` — `type BillingInterval = "monthly" | "yearly"`; `PLAN_LIMITS = { free: { items: 50, collections: 3 }, pro: { items: Infinity, collections: Infinity } } as const`; `priceIdForInterval(interval)` → `STRIPE_PRICE_ID_MONTHLY`/`_YEARLY` env or `null` (read at call time); `intervalForPriceId(priceId)` reverse lookup → `"monthly"`/`"yearly"`/`null` (also `null` for `""` / unset env); `isActiveStatus(status)` → `true` only for `"active"`/`"trialing"`. `limits.ts` — `interface LimitCheck { allowed: boolean; limit: number | null; current: number }`; `checkItemLimit(userId, isPro)` — Pro returns `{ allowed: true, limit: null, current: 0 }` with **no** `prisma.item.count` call, free path counts `prisma.item.count({ where: { userId } })` and returns `allowed: current < limit` (strict `<`, so the check run *before* an insert blocks the 51st at exactly 50); `checkCollectionLimit(userId, isPro)` mirrors against `prisma.collection.count` / `PLAN_LIMITS.free.collections` (3); `limitErrorMessage("item"|"collection", check)` → `"You've reached the free plan limit of N <nouns>. Upgrade to DevStash Pro for unlimited <nouns>."`. Helpers are **`userId`-first** (explicit arg, not `getDemoUserId()`) so they're ready for session-scoped data in Phase 2. **Validation**: `src/lib/validations/billing.ts` — `checkoutSchema = z.object({ interval: z.enum(["monthly", "yearly"]) })` (Zod v4) + `type CheckoutInput`. **Data layer**: `getUserItemCount(userId): Promise<number>` appended to `src/lib/db/items.ts` (after `getItemStats`) and `getUserCollectionCount(userId): Promise<number>` to `src/lib/db/collections.ts` (after `deleteCollection`) — both plain `prisma.<model>.count({ where: { userId } })`, no demo-user fallback; the pre-existing demo-scoped `getCollectionCount()` left untouched. Phase 2's create-gate uses these (a caller that already has the count can skip the limits module's own query). **`.env.example`**: the bare `# Stripe` + five `STRIPE_*="" ` stubs (previously an uncommitted change on `main`) replaced with the spec's documented comment block (per-var purpose, "leave SECRET/WEBHOOK blank to disable → 503") and committed with this phase. **Tests +22** (`src/lib/**` → auto-collected): `stripe/plans.test.ts` (6) — `priceIdForInterval`/`intervalForPriceId` round-trip + unknown-id + unset-env → `null` (all under `vi.stubEnv` + `afterEach(vi.unstubAllEnvs)`), `isActiveStatus` truth table, `PLAN_LIMITS.free` shape + `pro` `Infinity`; `stripe/limits.test.ts` (12, the required usage-limits suite) — `vi.mock("@/lib/prisma", …)` with `item.count`/`collection.count` spies, Pro short-circuit asserts `count` **not** called, free 10<50 → allowed, 50/51 → blocked, collection 2<3 allowed / 3 blocked, `limitErrorMessage` contains the number + "DevStash Pro"; `validations/billing.test.ts` (4) — `monthly`/`yearly` parse, `weekly`/`{}`/`5` reject. `npm run test` → **292 pass / 38 files** (was 270). `npm run lint` clean (only the pre-existing `prototypes/homepage/script.js` unused-`i` warning). `npm run build` green — first pass failed on `Stripe.LATEST_API_VERSION` (TS2339), fixed to `Stripe.API_VERSION`; `build` runs `prisma generate` which picked up the new columns. **Sanity check** (scratch script): with `STRIPE_SECRET_KEY` deleted, `getStripe()` returns `null` on 3 consecutive calls and logs `[stripe] STRIPE_SECRET_KEY not set — subscription billing is disabled.` exactly **once**; `isStripeConfigured()` → `false`. **Also committed with this phase** (were pre-existing untracked spec/plan artifacts on `main`, belong with this work): `context/features/stripe-phase-1-spec.md`, `context/features/stripe-phase-2-spec.md`, `context/research/stripe-integration-research.md`, `docs/stripe-integration-plan.md`. **Still deliberately excluded** (unrelated, carried from earlier features): `.claude/agents/ui-reviewer.md` (untracked). Deferred to Phase 2: Stripe webhook handler + signature verification, hosted Checkout session + `/api/billing/*` routes, the customer-portal link, wiring `checkItemLimit`/`checkCollectionLimit` into the item/collection create paths (and the decision to gate on the session user's counts vs. `getDemoUserId()` as an interim), the upgrade/pricing UI + Pro-state badges, and `prisma migrate deploy` on the production branch.
- Stripe Phase 2 — Integration & UI — Per `context/features/stripe-phase-2-spec.md`. Ships the working **DevStash Pro** subscription on top of Phase 1's `src/lib/stripe/*`; **no schema change** (Phase 1 already added all `stripe*` columns). **Scope decision (LOCKED): feature-gating counts the SESSION user** (`checkItemLimit(session.user.id, Boolean(session.user.isPro))`), the spec's recommended path — correct for real billing, though the dashboard data layer still writes items/collections under the demo user (`getDemoUserId()`), so a signed-in non-demo user's own row count is 0 and the limit is effectively inert until the data layer is session-scoped (accepted; recorded in the working file). **Session `isPro`** — `src/auth.ts` `jwt()` callback is now `async` and, after `token.id`, unconditionally `prisma.user.findUnique({ where: { id }, select: { isPro: true } })` → `token.isPro`; `session()` sets `session.user.isPro = Boolean(token.isPro)`. This is one indexed read per `auth()` call — deliberate: the webhook can't reach an already-issued JWT, so a page reload after checkout is what surfaces Pro. `src/types/next-auth.d.ts` adds `isPro: boolean` to `Session["user"]` + `isPro?: boolean` to `JWT`. The edge `proxy.ts` is untouched (different NextAuth instance, no Prisma — can't see `token.isPro`; Pro gating stays in pages/actions/routes). **New `src/lib/stripe/subscription.ts`** — `syncSubscriptionForCustomer(customerId)`, the **sole writer** of `isPro` / `stripeSubscriptionId` / `stripePriceId` / `stripeCurrentPeriodEnd`. Looks up the `User` by `stripeCustomerId` (warns + returns for an unknown customer), re-reads `stripe.subscriptions.list({ customer, status: "all", limit: 1 })`, and reconciles: no sub / `canceled` / `incomplete_expired` → clear all four; otherwise `isPro = isActiveStatus(sub.status)`, `stripePriceId = sub.items.data[0].price.id`, `stripeCurrentPeriodEnd = new Date(item.current_period_end * 1000)`. **Gotcha fixed:** in the pinned API version `2026-08-26.dahlia`, `current_period_end` was **removed from the Subscription object** and lives on the subscription *item* (`sub.items.data[0].current_period_end`) — the plan doc's `sub.current_period_end` is a TS error in SDK v22. Idempotent + order-independent by construction (always re-reads Stripe) — no processed-event table for v1. No-ops when `getStripe()` is `null`. **New API routes** (all mirror `src/app/api/collections/route.ts` conventions + `{ success, error }` envelope; all **fail soft → `503`** when `!getStripe() || !isStripeConfigured()`): `POST /api/stripe/webhook` — `export const runtime = "nodejs"`, **public** (deliberately NOT added to `src/proxy.ts`), reads the **raw** body via `await request.text()` (never `request.json()`), `stripe.webhooks.constructEvent(payload, sig, secret)`; missing/garbage signature → `400` with no DB write; `switch (event.type)` routes `checkout.session.completed` (→ `session.customer`), `customer.subscription.{created,updated,deleted}` (→ `sub.customer`), `invoice.payment_failed` (→ `invoice.customer`) all into `syncSubscriptionForCustomer`, default ignored; handler throw → `500` (Stripe retries); success → `{ received: true }`. `POST /api/stripe/checkout` — `{ interval: "monthly" | "yearly" }` (`checkoutSchema` from Phase 1) → `auth()` `401` → `503` → `readJsonBody`/`INVALID_JSON` → `safeParse` → `priceIdForInterval` (`503` if unset) → load user (`404` if gone, `409` if already `isPro`) → reuse `stripeCustomerId` else `stripe.customers.create({ email, metadata: { userId } })` **and persist it before returning** (a retried upgrade then reuses it — no duplicate customers) → `stripe.checkout.sessions.create({ mode: "subscription", customer, line_items: [{ price, quantity: 1 }], client_reference_id: userId, subscription_data: { metadata: { userId } }, allow_promotion_codes: true, success_url: "<origin>/settings?checkout=success", cancel_url: "<origin>/settings?checkout=cancelled" })` (`<origin>` = `getBaseUrl(request)`) → `{ success, data: { url } }`. `POST /api/stripe/portal` — `auth()` `401` → `503` → `409` if no `stripeCustomerId` → `stripe.billingPortal.sessions.create({ customer, return_url: "<origin>/settings" })` → `{ success, data: { url } }`. **Feature gating (server-side; UI is cosmetic):** `src/actions/items.ts` `createItem` — after `auth()` + Zod, before the query: `checkItemLimit(session.user.id, isPro)` → `{ success: false, error: limitErrorMessage("item", limit) }`, plus `parsed.data.type === "file" && !isPro` → `"File uploads are a Pro feature. Upgrade to attach files."`. `src/app/api/collections/route.ts` `POST` — `checkCollectionLimit(session.user.id, Boolean(session.user.isPro))` → `403` with `limitErrorMessage("collection", check)`. `src/app/api/upload/route.ts` — once `kind` is parsed, `kind === "file" && !session.user.isPro` → `403 "File uploads are a Pro feature."`; `kind === "image"` stays open. `src/app/api/auth/delete-account/route.ts` — selects `stripeSubscriptionId` too and, when `getStripe()` + a sub id, best-effort `stripe.subscriptions.cancel(id)` in a `try/catch` (a failure only logs — never blocks the delete; orphan customer at worst) before `prisma.user.delete`. **UI:** new `src/components/settings/BillingSection.tsx` (`"use client"`, props `{ isPro, hasCustomer }`) — free state: monthly/yearly segmented toggle (labels from `PRICING` in `src/lib/home-content.ts`) + **Upgrade to Pro** → `postJson("/api/stripe/checkout", { interval })` → `window.location.assign(res.data.data.url)`; Pro state: **Manage billing** → `postJson("/api/stripe/portal")` (disabled when `!hasCustomer`); `sonner` toast on non-OK / network failure (`res.status === 0`); card chrome `rounded-xl border border-border bg-card p-4` matching the other `/settings` cards. `postJson`'s shape is `{ ok, status, data }` where `data` is the parsed body, so the URL is `res.data?.data?.url` (adjusted from the plan doc's `res.data.url`). New `src/components/settings/BillingCheckoutToast.tsx` (`"use client"`, `useRef` one-shot) fires a `sonner` toast for `?checkout=success|cancelled`. `src/app/settings/page.tsx` — now takes `searchParams: Promise<{ checkout?: string | string[] }>`, resolves it, renders `<BillingCheckoutToast>` when the param is `success`/`cancelled`, and adds a **"Plan & billing"** `<section>` (with `<BillingSection isPro={user.isPro} hasCustomer={user.hasStripeCustomer} />`) between "Editor preferences" and "Account". `src/lib/db/profile.ts` — `getProfileUser` select gains `stripeCustomerId: true`; `ProfileUser` exposes `hasStripeCustomer: boolean` (derived — the raw id is destructured out and never returned); `profile.test.ts` fixtures + assertions updated (`stripeCustomerId: null` in, `hasStripeCustomer: false` out, `not.toHaveProperty("stripeCustomerId")`). `src/components/items/NewItemDialog.tsx` — new **required** `isPro` prop; a `PRO_ITEM_TYPES = new Set(["file"])`; when `!isPro` the `file` type pill is `disabled` with an outline "PRO" `Badge` and a "File uploads are a Pro feature. Upgrade" line linking to `/settings`; `image` stays enabled; if `initialType` is a locked type for a free user it falls back to `"snippet"` (`startType`). `isPro` threaded `dashboard/layout.tsx` (`Boolean(session?.user?.isPro)`) → `DashboardShell` → `TopBar` → `NewItemDialog`, and separately `/items/[type]/page.tsx` (added `auth()` to its `Promise.all`) → `NewTypeItemButton` → `NewItemDialog`. `src/components/home/PricingPlans.tsx` — optional item #9: new `signedIn` prop (threaded `src/app/page.tsx` → `PricingSection` → `PricingPlans`); a signed-in visitor's Pro "Go Pro" button POSTs `/api/stripe/checkout` with the toggled `period` and redirects (409 → "You're already on Pro." toast), signed-out keeps `<Link href="/register">`; Free CTA becomes "Go to dashboard" → `/dashboard` when signed in. **Tests +3** → `npm run test` **295 pass / 38 files** (was 292): `src/actions/items.test.ts` now `vi.mock`s `@/lib/stripe/limits` (`checkItemLimit` spy + a stub `limitErrorMessage`) so the action test never touches Prisma; default session is `{ id: "user_1", isPro: false }` with `checkItemLimit` → `{ allowed: true }`; the existing "forwards the upload fields for a file item" test now sets an `isPro: true` session; +3 new cases — free user at the item limit is blocked (query not called), a `file` item for a non-Pro user is blocked with the Pro message, and `isPro` is passed through to `checkItemLimit`. `profile.test.ts` got the fixture updates (no new `it`). No new suites for checkout/portal/webhook/`subscription.ts` (API/SDK surface — integration-verified per repo convention). `npm run lint` clean (only the pre-existing `prototypes/homepage/script.js` unused-`i` warning), `npm run build` green — first build failed on `sub.current_period_end` (TS2339, see the gotcha above), fixed to the subscription item; `/api/stripe/{checkout,portal,webhook}` build as new `ƒ` routes. **Browser-verified** (Playwright, demo/free session, live dev server, **Stripe env unset** — the deep integration path needs test-mode keys and is left for manual verification): `POST /api/stripe/webhook` (no signature) → `503 {"error":"Billing is not configured."}` and publicly reachable (not redirected to `/sign-in`); `POST /api/stripe/checkout` + `/portal` unauthed → `401`; `/settings` renders the "Plan & billing" card in the free state (monthly `$8/mo` / yearly `$72/yr` toggle + "Upgrade to Pro"); clicking "Upgrade to Pro" → `503` → error toast, button recovers from "Redirecting…"; the New Item dialog shows the `file` pill `disabled` with a "PRO" badge + "Upgrade" link to `/settings`, `image` enabled, "Create item" disabled; the dashboard still renders fine with the extra per-`auth()` `findUnique`. **Committed** on `feature/stripe-phase-2` → merged `--no-ff` to `main`, branch deleted. **Deliberately excluded** (unrelated, untracked, carried from earlier features): `.claude/agents/ui-reviewer.md`. **Deferred / needs manual completion:** the full Stripe CLI integration checklist (Dashboard product/two prices/portal/webhook setup, `stripe listen`, real `checkout.session.completed` → `isPro` flip + `stripeCustomerId`/`stripeSubscriptionId`/`stripePriceId`/`stripeCurrentPeriodEnd` populated on the Neon `development` branch, reload → session Pro, yearly price, 50-item/3-collection limits with a real signed-in user once the data layer is session-scoped, `file` upload 403 vs `image`, portal Cancel → `customer.subscription.updated` then `stripe trigger customer.subscription.deleted` → `isPro` false + fields cleared, `invoice.payment_failed` → `past_due` reconcile, `stripe events resend` idempotency + out-of-order delivery, bad-signature `400` with a real `whsec_`, no-customer portal `409`, already-Pro checkout `409`); `prisma migrate deploy` on the production branch (still pending from Phase 1); session-scoping the dashboard data layer so the limit checks actually bite for non-demo users; cross-route live `isPro` sync without a reload.
- Code language selector — the free-text "Language" `<Input>` (snippet / command items) replaced with a `<select>` dropdown, moved **above** the Content editor in both `NewItemDialog` and the drawer's `ItemEditForm`. Selecting a language drives Monaco syntax highlighting live as you type — the value already threads `ItemContentField` → `CodeEditor`, so the change is UX (dropdown + placement), not new wiring. **`src/lib/code-editor.ts`** gained `CODE_LANGUAGE_OPTIONS` (25 languages, each `value` a canonical Monaco id so `toMonacoLanguage` passes it straight through, leading `{ value: "", label: "Plain text" }`), `isKnownCodeLanguage(value)`, and `codeLanguageSelectValue(raw)` (trims; resolves a `LANGUAGE_ALIASES` entry like `bash`→`shell` / `ts`→`typescript` when it lands on a known option; otherwise returns the trimmed original). **New `src/components/items/item-form/LanguageSelect.tsx`** — a styled `<select>` (matches the settings editor-preferences `selectClass`); a stored value that isn't a known option (legacy free text) is kept as an extra leading `<option>` so editing an old item never drops it. **`NewItemDialog` / `ItemEditForm`** — the `showLanguage` `<Field>` moved to just before the `showContent` block and its `<Input>` swapped for `<LanguageSelect>`; both already passed `language` into `ItemContentField`. **No schema / server-action / validation change** — `language` stays `nullableText`, so the "Plain text" option (`""`) collapses to `null`. **Tests +8** (`code-editor.test.ts`: `CODE_LANGUAGE_OPTIONS` shape + unique lowercased values + every value round-trips through `toMonacoLanguage`; `isKnownCodeLanguage`; `codeLanguageSelectValue` blank / known / alias / passthrough) → `npm run test` **303 pass / 38 files**; `npm run lint` clean (only the pre-existing `prototypes/homepage/script.js` unused-`i` warning); `npm run build` green. Browser-verified (Playwright, demo session, live dev server): New Item modal — dropdown renders above the editor, picking TypeScript highlights `const`/`function`/strings as you type and the editor header label tracks it, "Plain text" clears highlighting live; edit drawer on the `groupBy utility` snippet — dropdown pre-selects the stored `typescript`, switching to Python re-tokenizes live; no console errors. Branch `feature/code-language-selector` was cut off `feature/upgrade-flow`, then rebased `--onto main` to drop the still-unmerged `ccdbd4d` upgrade-flow commit before merging `--no-ff` to `main` (one clean feature commit); `NewItemDialog`'s `/settings`-vs-`/upgrade` Pro link stayed at `main`'s `/settings` since the rebase dropped upgrade-flow. **Deliberately excluded** (unrelated, pre-existing on the tree): `.env.example`, `prisma/seed.ts`, `scripts/test-db.ts`, `src/app/sign-in/page.tsx`, `src/app/register/page.tsx`, `src/proxy.ts`; untracked `.claude/agents/ui-reviewer.md`, `context/features/ai-auto-tag-spec.md`, `context/features/ai-explain-spec.md`, `context/research/ai-integration-research.md`, `docs/ai-integration-plan.md`. Deferred: `feature/upgrade-flow` still needs its own `/feature complete`; the language list is fixed (no free-text / custom entry once switched away).
